## 6.5. 标量参数（Scalar Parameters）

在旧版 cuBLAS API 中，标量参数通过值传递（pass by value）从主机端传入。另外，少数会返回标量结果的函数（如 `dot()` 和 `nrm2()`）会在主机端返回计算结果，因此这些例程在返回之前需要等待设备上的内核执行完成，这使得与流的并行化变得不切实际。然而，大多数函数不返回任何值，以便与 Fortran 和现有的 BLAS 库保持更好的兼容性。