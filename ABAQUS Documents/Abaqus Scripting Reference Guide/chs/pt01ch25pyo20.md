# 25.20 ContactInitialization 对象

ContactInitialization 对象是其他 ContactInitialization 对象的抽象基类型。ContactInitialization 对象没有显式构造函数、成员或方法。

**访问**

```
import interaction
mdb.models[*name*].contactInitializations[*name*]
```

### 25.20.1 成员

ContactInitialization 对象没有成员。





