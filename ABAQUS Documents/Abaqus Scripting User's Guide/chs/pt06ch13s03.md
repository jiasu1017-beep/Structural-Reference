# 13.3 菜单栏





菜单栏由以下三个区域组成：
- 持久工具集菜单
- 模块菜单
- 帮助菜单

持久工具集菜单和帮助菜单在应用程序首次启动时显示，并在整个用户会话期间保持可见。模块菜单反映当前模块，并在用户访问各个模块时换入换出。你可以使用以下语句访问菜单栏：
```
menubar = getAFXApp().getAFXMainWindow().getMenubar()
```



