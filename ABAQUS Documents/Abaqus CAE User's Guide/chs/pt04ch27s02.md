# 27.2 我需要为显示体划分网格吗？





您不必为显示体划分网格。但是，要在可视化模块中显示部件实例，Abaqus/CAE会计算一个三角形网格来模拟部件实例的表面。此内部网格会被写入输入文件和结果输出数据库；但是，它仅用于渲染显示，不包含在数值分析中。默认情况下，可视化模块仅显示网格化显示体部件实例的自由边缘，但您可以更改显示的可见边缘。

同样，如果您确实为显示体划分了网格，或者使用网格部件创建显示体，Abaqus/CAE会在生成输入文件时保留您的网格；但是，它仅用于在可视化模块中渲染显示，不包含在数值分析中。

