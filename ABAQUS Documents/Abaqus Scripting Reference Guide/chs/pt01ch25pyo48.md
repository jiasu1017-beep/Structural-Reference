# 25.48 InteractionProperty 对象

InteractionProperty 对象是其他 InteractionProperty 对象的抽象基类型。InteractionProperty 对象没有显式构造函数、成员或方法。

**访问**

```
import interaction
mdb.models[*name*].interactionProperties[*name*]
```

### 25.48.1 成员

InteractionProperty 对象没有成员。





