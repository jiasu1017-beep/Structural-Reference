# 40.29 MdbDataInstance 对象

MdbDataInstance 对象实例数据。它对应于 cae 模型中同名的具有网格的零件实例。

**访问**

```
import visualization
session.mdbData[*name*].instances[*i*]
```

### 40.29.1 成员

MdbDataInstance 对象具有以下成员：

*name*

 String，指定实例名称。此属性为只读。

