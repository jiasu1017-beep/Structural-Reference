# 9 Boundary Condition 命令

每种类型的边界条件对象和每种类型的边界条件状态对象都是为特定类型的边界条件设计的。BoundaryCondition 对象存储边界条件的非传播数据以及相应 BoundaryConditionState 对象的多个实例，每个实例存储边界条件在单个步骤中的传播数据。BoundaryConditionState 对象的实例由其相应的 BoundaryCondition 对象内部创建和删除。
