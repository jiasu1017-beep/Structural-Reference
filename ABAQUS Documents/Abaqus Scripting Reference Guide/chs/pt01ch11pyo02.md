# 11.2 DrawingArea 对象

DrawingArea 对象指定用于放置视口的绘图区域的位置和大小。

**访问**

```
session.drawingArea
```

### 11.2.1 成员

DrawingArea 对象具有以下成员：

*width*

Float，指定宽度（毫米）。

*height*

Float，指定高度（毫米）。

*origin*

Float 对，指定左下角的坐标（毫米）。
