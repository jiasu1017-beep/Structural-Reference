## 2.7. cuBLAS Level-3 函数参考

在本章中，我们描述执行矩阵-矩阵运算的 Level-3 基本线性代数子程序（BLAS3）函数。

### 2.7.1. cublas<t>gemm()

此函数支持 64 位整数接口。

此函数执行矩阵-矩阵乘法运算

$C = \alpha\text{op}(A)\text{op}(B) + \beta C$

其中 $\alpha$ 和 $\beta$ 是标量，$A$、$B$ 和 $C$ 是以列优先格式存储的矩阵，维度分别为 $\text{op}(A)$ $m \times k$、$\text{op}(B)$ $k \times n$ 和 $C$ $m \times n$。另外，对于矩阵 $A$

$\text{op}(A) = \left\{ \begin{matrix}
A & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
A^{T} & {\text{if }\textsf{transa == CUBLAS\_OP\_T}} \\
A^{H} & {\text{if }\textsf{transa == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

$\text{op}(B)$ 对矩阵 $B$ 的定义与此类似。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| transa |  | input | 运算 op(A)，为非转置或（共轭）转置。 |
| transb |  | input | 运算 op(B)，为非转置或（共轭）转置。 |
| m |  | input | 矩阵 op(A) 和 C 的行数。 |
| n |  | input | 矩阵 op(B) 和 C 的列数。 |
| k |  | input | op(A) 的列数和 op(B) 的行数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, m)，否则为 lda × m 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × n，当 transb == CUBLAS_OP_N 时 ldb >= max(1, k)，否则为 ldb × k 且 ldb >= max(1, n)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, m)。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下表所示。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0 或 k < 0，或如果 transa 和 transb 不是 CUBLAS_OP_N、CUBLAS_OP_C、CUBLAS_OP_T 之一，或如果当 transa == CUBLAS_OP_N 时 lda < max(1, m) 且其他情况下 lda < max(1, k)，或如果当 transb == CUBLAS_OP_N 时 ldb < max(1, k) 且其他情况下 ldb < max(1, n)，或如果 ldc < max(1, m)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_ARCH_MISMATCH | 对于 cublasHgemm()，设备不支持半精度数学运算。 |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[sgemm()](http://www.netlib.org/blas/sgemm.f)、[dgemm()](http://www.netlib.org/blas/dgemm.f)、[cgemm()](http://www.netlib.org/blas/cgemm.f)、[zgemm()](http://www.netlib.org/blas/zgemm.f)

### 2.7.2. cublas<t>gemm3m()

此函数支持 64 位整数接口。

此函数执行复数矩阵-矩阵乘法，使用高斯复杂度归约算法。这可使性能提升高达 25%

$C = \alpha\text{op}(A)\text{op}(B) + \beta C$

其中 $\alpha$ 和 $\beta$ 是标量，$A$、$B$ 和 $C$ 是以列优先格式存储的矩阵，维度分别为 $\text{op}(A)$ $m \times k$、$\text{op}(B)$ $k \times n$ 和 $C$ $m \times n$。另外，对于矩阵 $A$

$\text{op}(A) = \left\{ \begin{matrix}
A & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
A^{T} & {\text{if }\textsf{transa == CUBLAS\_OP\_T}} \\
A^{H} & {\text{if }\textsf{transa == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

$\text{op}(B)$ 对矩阵 $B$ 的定义与此类似。

> **注意**

注意
这两个例程仅在计算能力等于或大于 5.0 的 GPU 上支持。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| transa |  | input | 运算 op(A)，为非转置或（共轭）转置。 |
| transb |  | input | 运算 op(B)，为非转置或（共轭）转置。 |
| m |  | input | 矩阵 op(A) 和 C 的行数。 |
| n |  | input | 矩阵 op(B) 和 C 的列数。 |
| k |  | input | op(A) 的列数和 op(B) 的行数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, m)，否则为 lda × m 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × n，当 transb == CUBLAS_OP_N 时 ldb >= max(1, k)，否则为 ldb × k 且 ldb >= max(1, n)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, m)。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下表所示：

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成。 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化。 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0 或 k < 0，或如果 transa 和 transb 不是 CUBLAS_OP_N、CUBLAS_OP_C、CUBLAS_OP_T 之一，或如果当 transa == CUBLAS_OP_N 时 lda < max(1, m) 且其他情况下 lda < max(1, k)，或如果当 transb == CUBLAS_OP_N 时 ldb < max(1, k) 且其他情况下 ldb < max(1, n)，或如果 ldc < max(1, m)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_ARCH_MISMATCH | 设备的计算能力低于 5.0。 |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动。 |

参考资料请参阅 NETLIB 文档：

[cgemm()](http://www.netlib.org/blas/cgemm.f)、[zgemm()](http://www.netlib.org/blas/zgemm.f)

### 2.7.3. cublas<t>gemmBatched()

此函数支持 64 位整数接口。

此函数执行一批矩阵的矩阵-矩阵乘法运算。该批次被认为是"一致的"，即所有实例对于各自的 A、B 和 C 矩阵具有相同的维度（m、n、k）、主维（lda、ldb、ldc）和转置（transa、transb）。批次中每个实例的输入矩阵和输出矩阵的地址从调用者传递给函数的指针数组中读取。

$C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack$

其中 $\alpha$ 和 $\beta$ 是标量，$A$、$B$ 和 $C$ 是指向以列优先格式存储的矩阵的指针数组，维度分别为 $\text{op}(A\lbrack i\rbrack)$ $m \times k$、$\text{op}(B\lbrack i\rbrack)$ $k \times n$ 和 $C\lbrack i\rbrack$ $m \times n$。另外，对于矩阵 $A$

$\text{op}(A) = \left\{ \begin{matrix}
A & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
A^{T} & {\text{if }\textsf{transa == CUBLAS\_OP\_T}} \\
A^{H} & {\text{if }\textsf{transa == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

$\text{op}(B\lbrack i\rbrack)$ 对矩阵 $B\lbrack i\rbrack$ 的定义与此类似。

> **注意**

注意
\(C\lbrack i\rbrack\) 矩阵不得重叠，即各个 gemm 操作必须能够独立计算；否则，将产生未定义行为。

在某些问题规模下，相比使用此 API，在不同的 CUDA 流中多次调用 cublas<t>gemm() 可能更有优势。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| transa |  | input | 运算 op(A[i])，为非转置或（共轭）转置。 |
| transb |  | input | 运算 op(B[i])，为非转置或（共轭）转置。 |
| m |  | input | 矩阵 op(A[i]) 和 C[i] 的行数。 |
| n |  | input | op(B[i]) 和 C[i] 的列数。 |
| k |  | input | op(A[i]) 的列数和 op(B[i]) 的行数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| Aarray | device | input | 指向 <type> 数组的指针数组，每个数组维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, m)，否则为 lda × m 且 lda >= max(1, k)。
所有指针必须满足特定的对齐要求。详见下文。 |
| lda |  | input | 用于存储每个矩阵 A[i] 的二维数组的主维。 |
| Barray | device | input | 指向 <type> 数组的指针数组，每个数组维度为 ldb × n，当 transb == CUBLAS_OP_N 时 ldb >= max(1, k)，否则为 ldb × k 且 ldb >= max(1, n)。
所有指针必须满足特定的对齐要求。详见下文。 |
| ldb |  | input | 用于存储每个矩阵 B[i] 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| Carray | device | in/out | 指向 <type> 数组的指针数组。维度为 ldc × n，ldc >= max(1, m)。矩阵 C[i] 不应重叠；否则，将产生未定义行为。
所有指针必须满足特定的对齐要求。详见下文。 |
| ldc |  | input | 用于存储每个矩阵 C[i] 的二维数组的主维。 |
| batchCount |  | input | Aarray、Barray 和 Carray 中包含的指针数量。 |

当使用 cublasSgemmBatched() 且数学模式启用快速数学模式时，放置在 GPU 内存中的指针（而非指针数组）必须正确对齐，以避免未对齐内存访问错误。理想情况下，所有指针应至少对齐到 16 字节。否则，建议它们满足以下规则：

- 如果 k % 4 == 0，则确保 intptr_t(ptr) % 16 == 0

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0 或 k < 0，或如果 transa 和 transb 不是 CUBLAS_OP_N、CUBLAS_OP_C、CUBLAS_OP_T 之一，或如果当 transa == CUBLAS_OP_N 时 lda < max(1, m) 且其他情况下 lda < max(1, k)，或如果当 transb == CUBLAS_OP_N 时 ldb < max(1, k) 且其他情况下 ldb < max(1, n)，或如果 ldc < max(1, m) |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |
| CUBLAS_STATUS_ARCH_MISMATCH | cublasHgemmBatched() 仅支持计算能力等于或大于 5.3 的 GPU |

### 2.7.4. cublas<t>gemmStridedBatched()

此函数支持 64 位整数接口。

此函数执行一批矩阵的矩阵-矩阵乘法运算。该批次被认为是"一致的"，即所有实例对于各自的 A、B 和 C 矩阵具有相同的维度（m、n、k）、主维（lda、ldb、ldc）和转置（transa、transb）。批次中每个实例的输入矩阵 A、B 和输出矩阵 C 相对于上一实例中的位置以元素数为单位的固定偏移量定位。用户将指向第一批实例的 A、B 和 C 矩阵的指针连同以元素数为单位的偏移量（strideA、strideB 和 strideC）一起传递给函数，这些偏移量决定了后续实例中输入和输出矩阵的位置。

$C + i*{strideC} = \alpha\text{op}(A + i*{strideA})\text{op}(B + i*{strideB}) + \beta(C + i*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack$

其中 $\alpha$ 和 $\beta$ 是标量，$A$、$B$ 和 $C$ 是指向以列优先格式存储的矩阵的指针数组，维度分别为 $\text{op}(A\lbrack i\rbrack)$ $m \times k$、$\text{op}(B\lbrack i\rbrack)$ $k \times n$ 和 $C\lbrack i\rbrack$ $m \times n$。另外，对于矩阵 $A$

$\text{op}(A) = \left\{ \begin{matrix}
A & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
A^{T} & {\text{if }\textsf{transa == CUBLAS\_OP\_T}} \\
A^{H} & {\text{if }\textsf{transa == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

$\text{op}(B\lbrack i\rbrack)$ 对矩阵 $B\lbrack i\rbrack$ 的定义与此类似。

> **注意**

注意
\(C\lbrack i\rbrack\) 矩阵不得重叠，即各个 gemm 操作必须能够独立计算；否则，将产生未定义行为。

在某些问题规模下，相比使用此 API，在不同的 CUDA 流中多次调用 cublas<t>gemm() 可能更有优势。

> **注意**

注意
在下表中，我们使用 A[i]、B[i]、C[i] 作为批次中第 i 个实例的 A、B 和 C 矩阵的表示法，隐含假定它们相对于 A[i-1]、B[i-1]、C[i-1] 分别以元素数为单位的偏移量 strideA、strideB、strideC。偏移量的单位是元素数，且不能为零。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| transa |  | input | 运算 op(A[i])，为非转置或（共轭）转置。 |
| transb |  | input | 运算 op(B[i])，为非转置或（共轭）转置。 |
| m |  | input | 矩阵 op(A[i]) 和 C[i] 的行数。 |
| n |  | input | op(B[i]) 和 C[i] 的列数。 |
| k |  | input | op(A[i]) 的列数和 op(B[i]) 的行数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | 指向批次第一个实例对应的 A 矩阵的 <type>* 指针，维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, m)，否则为 lda × m 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储每个矩阵 A[i] 的二维数组的主维。 |
| strideA |  | input | long long int 类型的值，表示 A[i] 和 A[i+1] 之间以元素数为单位的偏移量 |
| B | device | input | 指向批次第一个实例对应的 B 矩阵的 <type>* 指针，维度为 ldb × n，当 transb == CUBLAS_OP_N 时 ldb >= max(1, k)，否则为 ldb × k 且 ldb >= max(1, n)。 |
| ldb |  | input | 用于存储每个矩阵 B[i] 的二维数组的主维。 |
| strideB |  | input | long long int 类型的值，表示 B[i] 和 B[i+1] 之间以元素数为单位的偏移量 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | 指向批次第一个实例对应的 C 矩阵的 <type>* 指针，维度为 ldc × n，ldc >= max(1, m)。矩阵 C[i] 不应重叠；否则，将产生未定义行为。 |
| ldc |  | input | 用于存储每个矩阵 C[i] 的二维数组的主维。 |
| strideC |  | input | long long int 类型的值，表示 C[i] 和 C[i+1] 之间以元素数为单位的偏移量 |
| batchCount |  | input | 批次中要执行的 GEMM 数量。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0 或 k < 0，或如果 transa 和 transb 不是 CUBLAS_OP_N、CUBLAS_OP_C、CUBLAS_OP_T 之一，或如果当 transa == CUBLAS_OP_N 时 lda < max(1, m) 且其他情况下 lda < max(1, k)，或如果当 transb == CUBLAS_OP_N 时 ldb < max(1, k) 且其他情况下 ldb < max(1, n)，或如果 ldc < max(1, m) |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |
| CUBLAS_STATUS_ARCH_MISMATCH | cublasHgemmStridedBatched() 仅支持计算能力等于或大于 5.3 的 GPU |

### 2.7.5. cublas<t>gemmGroupedBatched()

此函数支持 64 位整数接口。

此函数对矩阵组执行矩阵-矩阵乘法运算。给定的组被认为是"一致的"，即所有实例对于各自的 A、B 和 C 矩阵具有相同的维度（m、n、k）、主维（lda、ldb、ldc）和转置（transa、transb）。但是，维度、主维、转置和比例因子（alpha、beta）可能因组而异。批次中每个实例的输入矩阵和输出矩阵的地址从调用者传递给函数的指针数组中读取。这在功能上等价于以下内容：

批次中每个实例的输入矩阵和输出矩阵的地址从调用者传递给函数的指针数组中读取。这在功能上等价于以下内容：

```c++

idx = 0;
for i = 0:group_count - 1
    for j = 0:group_size[i] - 1
        gemm(transa_array[i], transb_array[i], m_array[i], n_array[i], k_array[i],
             alpha_array[i], Aarray[idx], lda_array[i], Barray[idx], ldb_array[i],
             beta_array[i], Carray[idx], ldc_array[i]);
        idx += 1;
    end
end

```

其中 $\text{alpha\_array}$ 和 $\text{beta\_array}$ 是比例因子数组，$\text{Aarray}$、$\text{Barray}$ 和 $\text{Carray}$ 是指向以列优先格式存储的矩阵的指针数组。对于属于组 $i$ 的给定索引 $\text{idx}$，维度如下：

> \(\text{op}(\text{Aarray}\lbrack\text{idx}\rbrack)\)：\(\text{m\_array}\lbrack i\rbrack \times \text{k\_array}\lbrack i\rbrack\)
\(\text{op}(\text{Barray}\lbrack\text{idx}\rbrack)\)：\(\text{k\_array}\lbrack i\rbrack \times \text{n\_array}\lbrack i\rbrack\)
\(\text{Carray}\lbrack\text{idx}\rbrack\)：\(\text{m\_array}\lbrack i\rbrack \times \text{n\_array}\lbrack i\rbrack\)

> **注意**

注意
此 API 接受两种不同长度的数组。维度、主维、转置和比例因子的数组长度为 group_count，矩阵数组的长度为 problem_count，其中 \(\text{problem\_count} = \sum_{i = 0}^{\text{group\_count} - 1} \text{group\_size}\lbrack i\rbrack\)

对于组 $i$ 中矩阵 $A[\text{idx}]$

$\text{op}(A[\text{idx}]) = \left\{ \begin{matrix}
A[\text{idx}] & {\text{if }\textsf{$\mathrm{transa\_array}\lbrack i\rbrack$ == CUBLAS\_OP\_N}} \\
A[\text{idx}]^{T} & {\text{if }\textsf{$\mathrm{transa\_array}\lbrack i\rbrack$ == CUBLAS\_OP\_T}} \\
A[\text{idx}]^{H} & {\text{if }\textsf{$\mathrm{transa\_array}\lbrack i\rbrack$ == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

$\text{op}(B[\text{idx}])$ 对组 $i$ 中矩阵 $B[\text{idx}]$ 的定义与此类似。

> **注意**

注意
\(C\lbrack\text{idx}\rbrack\) 矩阵不得重叠，即各个 gemm 操作必须能够独立计算；否则，将产生未定义行为。

在某些问题规模下，相比使用此 API，在不同的 CUDA 流中多次调用 cublas<t>gemmBatched() 可能更有优势。

| 参数 | 内存 | 输入/输出 | 含义 | 数组长度 |
| --- | --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |  |
| transa_array | host | input | 每个组的运算 op(A[idx])，为非转置或（共轭）转置。 | group_count |
| transb_array | host | input | 每个组的运算 op(B[idx])，为非转置或（共轭）转置。 | group_count |
| m_array | host | input | 包含每个组的矩阵 op(A[idx]) 和 C[idx] 行数的数组。 | group_count |
| n_array | host | input | 包含每个组的 op(B[idx]) 和 C[idx] 列数的数组。 | group_count |
| k_array | host | input | 包含每个组的 op(A[idx]) 列数和 op(B[idx]) 行数的数组。 | group_count |
| alpha_array | host | input | 包含每个组用于乘法的 <type> 标量的数组。 | group_count |
| Aarray | device | input | 指向 <type> 数组的指针数组，每个数组维度为 lda[i] × k[i]，当 transa[i] == CUBLAS_OP_N 时 lda[i] >= max(1, m[i])，否则为 lda[i] × m[i] 且 lda[i] >= max(1, k[i])。
所有指针必须满足特定的对齐要求。详见下文。 | problem_count |
| lda_array | host | input | 包含每个组用于存储每个矩阵 A[idx] 的二维数组主维的数组。 | group_count |
| Barray | device | input | 指向 <type> 数组的指针数组，每个数组维度为 ldb[i] × n[i]，当 transb[i] == CUBLAS_OP_N 时 ldb[i] >= max(1, k[i])，否则为 ldb[i] × k[i] 且 ldb[i] >= max(1, n[i])。
所有指针必须满足特定的对齐要求。详见下文。 | problem_count |
| ldb_array | host | input | 包含每个组用于存储每个矩阵 B[idx] 的二维数组主维的数组。 | group_count |
| beta_array | host | input | 包含每个组用于乘法的 <type> 标量的数组。 | group_count |
| Carray | device | in/out | 指向 <type> 数组的指针数组。维度为 ldc[i] × n[i]，ldc[i] >= max(1, m[i])。矩阵 C[idx] 不应重叠；否则，将产生未定义行为。
所有指针必须满足特定的对齐要求。详见下文。 | problem_count |
| ldc_array | host | input | 包含每个组用于存储每个矩阵 C[idx] 的二维数组主维的数组。 | group_count |
| group_count | host | input | 组的数量 |  |
| group_size | host | input | 包含每个组的 Aarray、Barray 和 Carray 中指针数量的数组。 | group_count |

当使用 cublasSgemmGroupedBatched() 且数学模式启用快速数学模式时，放置在 GPU 内存中的指针（而非指针数组）必须正确对齐，以避免未对齐内存访问错误。理想情况下，所有指针应至少对齐到 16 字节。否则，需要它们满足以下规则：

- 如果 k % 4 == 0，则确保 intptr_t(ptr) % 16 == 0

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 transa_array、transb_array、m_array、n_array、k_array、alpha_array、lda_array、ldb_array、beta_array、ldc_array 或 group_size 为 NULL，或如果 group_count < 0，或如果 m_array[i] < 0、n_array[i] < 0、k_array[i] < 0、group_size[i] < 0，或如果 transa_array[i] 和 transb_array[i] 不是 CUBLAS_OP_N、CUBLAS_OP_C、CUBLAS_OP_T 之一，或如果当 transa_array[i] == CUBLAS_OP_N 时 lda_array[i] < max(1, m_array[i]) 且其他情况下 lda_array[i] < max(1, k_array[i])，或如果当 transb_array[i] == CUBLAS_OP_N 时 ldb_array[i] < max(1, k_array[i]) 且其他情况下 ldb_array[i] < max(1, n_array[i])，或如果 ldc_array[i] < max(1, m_array[i]) |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |
| CUBLAS_STATUS_NOT_SUPPORTED | 指针模式设置为 CUBLAS_POINTER_MODE_DEVICE |

### 2.7.6. cublas<t>symm()

此函数支持 64 位整数接口。

此函数执行对称矩阵-矩阵乘法

$C = \left\{ \begin{matrix}
{\alpha AB + \beta C} & {\text{if }\textsf{side == CUBLAS\_SIDE\_LEFT}} \\
{\alpha BA + \beta C} & {\text{if }\textsf{side == CUBLAS\_SIDE\_RIGHT}} \\
\end{matrix} \right.$

其中 $A$ 是以下三角或上三角模式存储的对称矩阵，$B$ 和 $C$ 是 $m \times n$ 矩阵，$\alpha$ 和 $\beta$ 是标量。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| side |  | input | 指示矩阵 A 是在 B 的左侧还是右侧。 |
| uplo |  | input | 指示矩阵 A 的下三角或上三角部分是否存储，另一对称部分不被引用，从存储的元素推断。 |
| m |  | input | 矩阵 C 和 B 的行数，矩阵 A 的尺寸相应调整。 |
| n |  | input | 矩阵 C 和 B 的列数，矩阵 A 的尺寸相应调整。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × m，当 side == CUBLAS_SIDE_LEFT 时 lda >= max(1, m)，否则为 lda × n 且 lda >= max(1, n)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × n，ldb >= max(1, m)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, m)。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0，或如果 side 不是 CUBLAS_SIDE_LEFT 和 CUBLAS_SIDE_RIGHT 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果当 side == CUBLAS_SIDE_LEFT 时 lda < max(1, m) 且其他情况下 lda < max(1, n)，或如果 ldb < max(1, m)，或如果 ldc < max(1, m)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[ssymm()](http://www.netlib.org/blas/ssymm.f)、[dsymm()](http://www.netlib.org/blas/dsymm.f)、[csymm()](http://www.netlib.org/blas/csymm.f)、[zsymm()](http://www.netlib.org/blas/zsymm.f)

### 2.7.7. cublas<t>syrk()

此函数支持 64 位整数接口。

此函数执行对称秩-$k$ 更新

$C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C$

其中 $\alpha$ 和 $\beta$ 是标量，$C$ 是以下三角或上三角模式存储的对称矩阵，$A$ 是维度为 $\text{op}(A)$ $n \times k$ 的矩阵。另外，对于矩阵 $A$

$\text{op}(A) = \left\{ \begin{matrix}
A & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
A^{T} & {\text{if }\textsf{transa == CUBLAS\_OP\_T}} \\
\end{matrix} \right.$

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| uplo |  | input | 指示矩阵 C 的下三角或上三角部分是否存储，另一对称部分不被引用，从存储的元素推断。 |
| trans |  | input | 运算 op(A)，为非转置或转置。 |
| n |  | input | 矩阵 op(A) 和 C 的行数。 |
| k |  | input | 矩阵 op(A) 的列数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × k，当 trans == CUBLAS_OP_N 时 lda >= max(1, n)，否则为 lda × n 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, n)。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 n < 0 或 k < 0，或如果 trans 不是 CUBLAS_OP_N、CUBLAS_OP_T 和 CUBLAS_OP_C 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果当 trans == CUBLAS_OP_N 时 lda < max(1, n) 且其他情况下 lda < max(1, k)，或如果 ldc < max(1, n)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[ssyrk()](http://www.netlib.org/blas/ssyrk.f)、[dsyrk()](http://www.netlib.org/blas/dsyrk.f)、[csyrk()](http://www.netlib.org/blas/csyrk.f)、[zsyrk()](http://www.netlib.org/blas/zsyrk.f)

### 2.7.8. cublas<t>syr2k()

此函数支持 64 位整数接口。

此函数执行对称秩-$2k$ 更新

$C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C$

其中 $\alpha$ 和 $\beta$ 是标量，$C$ 是以下三角或上三角模式存储的对称矩阵，$A$ 和 $B$ 分别是维度为 $\text{op}(A)$ $n \times k$ 和 $\text{op}(B)$ $n \times k$ 的矩阵。另外，对于矩阵 $A$ 和 $B$

$\text{op(}A\text{) and op(}B\text{)} = \left\{ \begin{matrix}
{A\text{ and }B} & {\text{if }\textsf{trans == CUBLAS\_OP\_N}} \\
{A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == CUBLAS\_OP\_T}} \\
\end{matrix} \right.$

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| uplo |  | input | 指示矩阵 C 的下三角或上三角部分是否存储，另一对称部分不被引用，从存储的元素推断。 |
| trans |  | input | 运算 op(A)，为非转置或转置。 |
| n |  | input | 矩阵 op(A)、op(B) 和 C 的行数。 |
| k |  | input | 矩阵 op(A) 和 op(B) 的列数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, n)，否则为 lda × n 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × k，当 transb == CUBLAS_OP_N 时 ldb >= max(1, n)，否则为 ldb × n 且 ldb >= max(1, k)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, n)。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 n < 0 或 k < 0，或如果 trans 不是 CUBLAS_OP_N、CUBLAS_OP_T 和 CUBLAS_OP_C 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果当 trans == CUBLAS_OP_N 时 lda < max(1, n) 且其他情况下 lda < max(1, k)，或如果当 trans == CUBLAS_OP_N 时 ldb < max(1, n) 且其他情况下 ldb < max(1, k)，或如果 ldc < max(1, n)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[ssyr2k()](http://www.netlib.org/blas/ssyr2k.f)、[dsyr2k()](http://www.netlib.org/blas/dsyr2k.f)、[csyr2k()](http://www.netlib.org/blas/csyr2k.f)、[zsyr2k()](http://www.netlib.org/blas/zsyr2k.f)

### 2.7.9. cublas<t>syrkx()

此函数支持 64 位整数接口。

此函数执行对称秩-$k$ 更新的变体

$C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C$

其中 $\alpha$ 和 $\beta$ 是标量，$C$ 是以下三角或上三角模式存储的对称矩阵，$A$ 和 $B$ 分别是维度为 $\text{op}(A)$ $n \times k$ 和 $\text{op}(B)$ $n \times k$ 的矩阵。另外，对于矩阵 $A$ 和 $B$

$\text{op(}A\text{) and op(}B\text{)} = \left\{ \begin{matrix}
{A\text{ and }B} & {\text{if }\textsf{trans == CUBLAS\_OP\_N}} \\
{A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == CUBLAS\_OP\_T}} \\
\end{matrix} \right.$

当 B 以确保结果是对称的方式给出时，可以使用此例程。一个常见的例子是当矩阵 B 是矩阵 A 的缩放形式：这等价于 B 是矩阵 A 与对角矩阵的乘积。有关高效计算普通矩阵与对角矩阵的乘积，请参阅例程 cublas<t>dgmm()。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| uplo |  | input | 指示矩阵 C 的下三角或上三角部分是否存储，另一对称部分不被引用，从存储的元素推断。 |
| trans |  | input | 运算 op(A)，为非转置或转置。 |
| n |  | input | 矩阵 op(A)、op(B) 和 C 的行数。 |
| k |  | input | 矩阵 op(A) 和 op(B) 的列数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, n)，否则为 lda × n 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × k，当 transb == CUBLAS_OP_N 时 ldb >= max(1, n)，否则为 ldb × n 且 ldb >= max(1, k)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, n)。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 n < 0 或 k < 0，或如果 trans 不是 CUBLAS_OP_N、CUBLAS_OP_T 和 CUBLAS_OP_C 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果当 trans == CUBLAS_OP_N 时 lda < max(1, n) 且其他情况下 lda < max(1, k)，或如果当 trans == CUBLAS_OP_N 时 ldb < max(1, n) 且其他情况下 ldb < max(1, k)，或如果 ldc < max(1, n)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[ssyrk()](http://www.netlib.org/blas/ssyrk.f)、[dsyrk()](http://www.netlib.org/blas/dsyrk.f)、[csyrk()](http://www.netlib.org/blas/csyrk.f)、[zsyrk()](http://www.netlib.org/blas/zsyrk.f) 和

[ssyr2k()](http://www.netlib.org/blas/ssyr2k.f)、[dsyr2k()](http://www.netlib.org/blas/dsyr2k.f)、[csyr2k()](http://www.netlib.org/blas/csyr2k.f)、[zsyr2k()](http://www.netlib.org/blas/zsyr2k.f)

### 2.7.10. cublas<t>trmm()

此函数支持 64 位整数接口。

此函数执行三角矩阵-矩阵乘法

$C = \left\{ \begin{matrix}
{\alpha\text{op}(A)B} & {\text{if }\textsf{side == CUBLAS\_SIDE\_LEFT}} \\
{\alpha B\text{op}(A)} & {\text{if }\textsf{side == CUBLAS\_SIDE\_RIGHT}} \\
\end{matrix} \right.$

其中 $A$ 是以下三角或上三角模式存储的三角矩阵（带或不带主对角线），$B$ 和 $C$ 是 $m \times n$ 矩阵，$\alpha$ 是标量。另外，对于矩阵 $A$

$\text{op}(A) = \left\{ \begin{matrix}
A & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
A^{T} & {\text{if }\textsf{transa == CUBLAS\_OP\_T}} \\
A^{H} & {\text{if }\textsf{transa == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

注意，为了实现更好的并行性，cuBLAS 与 BLAS API 的区别仅在此例程中。BLAS API 假定原地实现（结果写回 B），而 cuBLAS API 假定原地实现（结果写入 C）。应用程序可以通过将矩阵 B 的地址传递给矩阵 C 来获得 BLAS API 的原地功能。不支持输入参数中的其他重叠。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| side |  | input | 指示矩阵 A 是在 B 的左侧还是右侧。 |
| uplo |  | input | 指示矩阵 A 的下三角或上三角部分是否存储，另一部分不被引用，从存储的元素推断。 |
| trans |  | input | 运算 op(A)，为非转置或（共轭）转置。 |
| diag |  | input | 指示矩阵 A 主对角线上的元素是否为单位 1 且不应被访问。 |
| m |  | input | 矩阵 B 的行数，矩阵 A 的尺寸相应调整。 |
| n |  | input | 矩阵 B 的列数，矩阵 A 的尺寸相应调整。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量，如果 alpha == 0，则不引用 A，且 B 不必是有效输入。 |
| A | device | input | <type> 数组，当 side == CUBLAS_SIDE_LEFT 时维度为 lda × m，lda >= max(1, m)，否则为 lda × n 且 lda >= max(1, n)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × n，ldb >= max(1, m)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, m)。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0，或如果 trans 不是 CUBLAS_OP_N、CUBLAS_OP_T 和 CUBLAS_OP_C 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果 side 不是 CUBLAS_SIDE_LEFT 和 CUBLAS_SIDE_RIGHT 之一，或如果当 side == CUBLAS_SIDE_LEFT 时 lda < max(1, m) 且其他情况下 lda < max(1, n)，或如果 ldb < max(1, m)，或如果 ldc < max(1, m)，或如果 alpha 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[strmm()](http://www.netlib.org/blas/strmm.f)、[dtrmm()](http://www.netlib.org/blas/dtrmm.f)、[ctrmm()](http://www.netlib.org/blas/ctrmm.f)、[ztrmm()](http://www.netlib.org/blas/ztrmm.f)

### 2.7.11. cublas<t>trsm()

此函数支持 64 位整数接口。

此函数求解具有多个右侧向量的三角线性系统

$\left\{ \begin{matrix}
{\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == CUBLAS\_SIDE\_LEFT}} \\
{X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == CUBLAS\_SIDE\_RIGHT}} \\
\end{matrix} \right.$

其中 $A$ 是以下三角或上三角模式存储的三角矩阵（带或不带主对角线），$X$ 和 $B$ 是 $m \times n$ 矩阵，$\alpha$ 是标量。另外，对于矩阵 $A$

$\text{op}(A) = \left\{ \begin{matrix}
A & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
A^{T} & {\text{if }\textsf{transa == CUBLAS\_OP\_T}} \\
A^{H} & {\text{if }\textsf{transa == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

解 $X$ 在退出时覆盖右侧向量 $B$。

此函数不包含奇异性或近奇异性测试。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| side |  | input | 指示矩阵 A 是在 X 的左侧还是右侧。 |
| uplo |  | input | 指示矩阵 A 的下三角或上三角部分是否存储，另一部分不被引用，从存储的元素推断。 |
| trans |  | input | 运算 op(A)，为非转置或（共轭）转置。 |
| diag |  | input | 指示矩阵 A 主对角线上的元素是否为单位 1 且不应被访问。 |
| m |  | input | 矩阵 B 的行数，矩阵 A 的尺寸相应调整。 |
| n |  | input | 矩阵 B 的列数，矩阵 A 的尺寸相应调整。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量，如果 alpha == 0，则不引用 A，且 B 不必是有效输入。 |
| A | device | input | <type> 数组，当 side == CUBLAS_SIDE_LEFT 时维度为 lda × m，lda >= max(1, m)，否则为 lda × n 且 lda >= max(1, n)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | in/out | <type> 数组。维度为 ldb × n，ldb >= max(1, m)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0，或如果 trans 不是 CUBLAS_OP_N、CUBLAS_OP_T 和 CUBLAS_OP_C 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果 side 不是 CUBLAS_SIDE_LEFT 和 CUBLAS_SIDE_RIGHT 之一，或如果 diag 不是 CUBLAS_DIAG_UNIT 和 CUBLAS_DIAG_NON_UNIT 之一，或如果当 side == CUBLAS_SIDE_LEFT 时 lda < max(1, m) 且其他情况下 lda < max(1, n)，或如果 ldb < max(1, m)，或如果 alpha 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[strsm()](http://www.netlib.org/blas/strsm.f)、[dtrsm()](http://www.netlib.org/blas/dtrsm.f)、[ctrsm()](http://www.netlib.org/blas/ctrsm.f)、[ztrsm()](http://www.netlib.org/blas/ztrsm.f)

### 2.7.12. cublas<t>trsmBatched()

此函数支持 64 位整数接口。

此函数求解具有多个右侧向量的三角线性系统数组

$\left\{ \begin{matrix}
{\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == CUBLAS\_SIDE\_LEFT}} \\
{X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == CUBLAS\_SIDE\_RIGHT}} \\
\end{matrix} \right.$

其中 $A\lbrack i\rbrack$ 是以下三角或上三角模式存储的三角矩阵（带或不带主对角线），$X\lbrack i\rbrack$ 和 $B\lbrack i\rbrack$ 是 $m \times n$ 矩阵，$\alpha$ 是标量。另外，对于矩阵 $A$

$\text{op}(A\lbrack i\rbrack) = \left\{ \begin{matrix}
{A\lbrack i\rbrack} & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
{A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == CUBLAS\_OP\_T}} \\
{A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

解 $X\lbrack i\rbrack$ 在退出时覆盖右侧向量 $B\lbrack i\rbrack$。

此函数不包含奇异性或近奇异性测试。

此函数适用于任意大小，但旨在用于矩阵规模较小、启动开销是重要因素的情况。对于较大规模，在一组 CUDA 流中多次调用常规 cublas<t>trsm() 可能更有优势。

当前实现仅限于计算能力大于或等于 2.0 的设备。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| side |  | input | 指示矩阵 A[i] 是在 X[i] 的左侧还是右侧。 |
| uplo |  | input | 指示矩阵 A[i] 的下三角或上三角部分是否存储，另一部分不被引用，从存储的元素推断。 |
| trans |  | input | 运算 op(A[i])，为非转置或（共轭）转置。 |
| diag |  | input | 指示矩阵 A[i] 主对角线上的元素是否为单位 1 且不应被访问。 |
| m |  | input | 矩阵 B[i] 的行数，矩阵 A[i] 的尺寸相应调整。 |
| n |  | input | 矩阵 B[i] 的列数，矩阵 A[i] 的尺寸相应调整。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量，如果 alpha == 0，则不引用 A[i]，且 B[i] 不必是有效输入。 |
| A | device | input | 指向 <type> 数组的指针数组，当 side == CUBLAS_SIDE_LEFT 时每个数组维度为 lda × m，lda >= max(1, m)，否则为 lda × n 且 lda >= max(1, n)。 |
| lda |  | input | 用于存储矩阵 A[i] 的二维数组的主维。 |
| B | device | in/out | 指向 <type> 数组的指针数组，每个数组维度为 ldb × n，ldb >= max(1, m)。矩阵 B[i] 不应重叠；否则，将产生未定义行为。 |
| ldb |  | input | 用于存储矩阵 B[i] 的二维数组的主维。 |
| batchCount |  | input | A 和 B 中包含的指针数量。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0，或如果 trans 不是 CUBLAS_OP_N、CUBLAS_OP_T 和 CUBLAS_OP_C 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果 side 不是 CUBLAS_SIDE_LEFT 和 CUBLAS_SIDE_RIGHT 之一，或如果 diag 不是 CUBLAS_DIAG_UNIT 和 CUBLAS_DIAG_NON_UNIT 之一，或如果当 side == CUBLAS_SIDE_LEFT 时 lda < max(1, m) 且其他情况下 lda < max(1, n)，或如果 ldb < max(1, m) |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[strsm()](http://www.netlib.org/blas/strsm.f)、[dtrsm()](http://www.netlib.org/blas/dtrsm.f)、[ctrsm()](http://www.netlib.org/blas/ctrsm.f)、[ztrsm()](http://www.netlib.org/blas/ztrsm.f)

### 2.7.13. cublas<t>hemm()

此函数支持 64 位整数接口。

此函数执行厄米特矩阵-矩阵乘法

$C = \left\{ \begin{matrix}
{\alpha AB + \beta C} & {\text{if }\textsf{side == CUBLAS\_SIDE\_LEFT}} \\
{\alpha BA + \beta C} & {\text{if }\textsf{side == CUBLAS\_SIDE\_RIGHT}} \\
\end{matrix} \right.$

其中 $A$ 是以下三角或上三角模式存储的厄米特矩阵，$B$ 和 $C$ 是 $m \times n$ 矩阵，$\alpha$ 和 $\beta$ 是标量。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| side |  | input | 指示矩阵 A 是在 B 的左侧还是右侧。 |
| uplo |  | input | 指示矩阵 A 的下三角或上三角部分是否存储，另一厄米特部分不被引用，从存储的元素推断。 |
| m |  | input | 矩阵 C 和 B 的行数，矩阵 A 的尺寸相应调整。 |
| n |  | input | 矩阵 C 和 B 的列数，矩阵 A 的尺寸相应调整。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，当 side == CUBLAS_SIDE_LEFT 时维度为 lda × m，lda >= max(1, m)，否则为 lda × n 且 lda >= max(1, n)。对角线元素的虚部假定为零。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × n，ldb >= max(1, m)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| beta |  | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, m)。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 m < 0 或 n < 0，或如果 side 不是 CUBLAS_SIDE_LEFT 和 CUBLAS_SIDE_RIGHT 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果当 side == CUBLAS_SIDE_LEFT 时 lda < max(1, m) 且其他情况下 lda < max(1, n)，或如果 ldb < max(1, m)，或如果 ldc < max(1, m)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[chemm()](http://www.netlib.org/blas/chemm.f)、[zhemm()](http://www.netlib.org/blas/zhemm.f)

### 2.7.14. cublas<t>herk()

此函数支持 64 位整数接口。

此函数执行厄米特秩-$k$ 更新

$C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C$

其中 $\alpha$ 和 $\beta$ 是标量，$C$ 是以下三角或上三角模式存储的厄米特矩阵，$A$ 是维度为 $\text{op}(A)$ $n \times k$ 的矩阵。另外，对于矩阵 $A$

$\text{op}(A) = \left\{ \begin{matrix}
A & {\text{if }\textsf{transa == CUBLAS\_OP\_N}} \\
A^{H} & {\text{if }\textsf{transa == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| uplo |  | input | 指示矩阵 C 的下三角或上三角部分是否存储，另一厄米特部分不被引用。 |
| trans |  | input | 运算 op(A)，为非转置或（共轭）转置。 |
| n |  | input | 矩阵 op(A) 和 C 的行数。 |
| k |  | input | 矩阵 op(A) 的列数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, n)，否则为 lda × n 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| beta |  | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, n)。对角线元素的虚部假定为零并设置为零。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 n < 0 或 k < 0，或如果 trans 不是 CUBLAS_OP_N、CUBLAS_OP_T 和 CUBLAS_OP_C 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果当 trans == CUBLAS_OP_N 时 lda < max(1, n) 且其他情况下 lda < max(1, k)，或如果 ldc < max(1, n)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[cherk()](http://www.netlib.org/blas/cherk.f)、[zherk()](http://www.netlib.org/blas/zherk.f)

### 2.7.15. cublas<t>her2k()

此函数支持 64 位整数接口。

此函数执行厄米特秩-$2k$ 更新

$C = \alpha\text{op}(A)\text{op}(B)^{H} + \overset{ˉ}{\alpha}\text{op}(B)\text{op}(A)^{H} + \beta C$

其中 $\alpha$ 和 $\beta$ 是标量，$C$ 是以下三角或上三角模式存储的厄米特矩阵，$A$ 和 $B$ 分别是维度为 $\text{op}(A)$ $n \times k$ 和 $\text{op}(B)$ $n \times k$ 的矩阵。另外，对于矩阵 $A$ 和 $B$

$\text{op(}A\text{) and op(}B\text{)} = \left\{ \begin{matrix}
{A\text{ and }B} & {\text{if }\textsf{trans == CUBLAS\_OP\_N}} \\
{A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| uplo |  | input | 指示矩阵 C 的下三角或上三角部分是否存储，另一厄米特部分不被引用。 |
| trans |  | input | 运算 op(A)，为非转置或（共轭）转置。 |
| n |  | input | 矩阵 op(A)、op(B) 和 C 的行数。 |
| k |  | input | 矩阵 op(A) 和 op(B) 的列数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, n)，否则为 lda × n 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × k，当 transb == CUBLAS_OP_N 时 ldb >= max(1, n)，否则为 ldb × n 且 ldb >= max(1, k)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的 <type> 标量。如果 beta == 0，则 C 不必是有效输入。 |
| C | device | in/out | <type> 数组，维度为 ldc × n，ldc >= max(1, n)。对角线元素的虚部假定为零并设置为零。 |
| ldc |  | input | 用于存储矩阵 C 的二维数组的主维。 |

此函数可能返回的错误值及其含义如下。

| 错误值 | 含义 |
| --- | --- |
| CUBLAS_STATUS_SUCCESS | 操作成功完成 |
| CUBLAS_STATUS_NOT_INITIALIZED | 库未初始化 |
| CUBLAS_STATUS_INVALID_VALUE | 如果 n < 0 或 k < 0，或如果 trans 不是 CUBLAS_OP_N、CUBLAS_OP_T 和 CUBLAS_OP_C 之一，或如果 uplo 不是 CUBLAS_FILL_MODE_LOWER 和 CUBLAS_FILL_MODE_UPPER 之一，或如果当 trans == CUBLAS_OP_N 时 lda < max(1, n) 且其他情况下 lda < max(1, k)，或如果 ldc < max(1, n)，或如果 alpha 或 beta 为 NULL，或如果 beta 不为零时 C 为 NULL |
| CUBLAS_STATUS_EXECUTION_FAILED | 函数无法在 GPU 上启动 |

参考资料请参阅 NETLIB 文档：

[cher2k()](http://www.netlib.org/blas/cher2k.f)、[zher2k()](http://www.netlib.org/blas/zher2k.f)

### 2.7.16. cublas<t>herkx()

此函数支持 64 位整数接口。

此函数执行厄米特秩-$k$ 更新的变体

$C = \alpha\text{op}(A)\text{op}(B)^{H} + \beta C$

其中 $\alpha$ 和 $\beta$ 是标量，$C$ 是以下三角或上三角模式存储的厄米特矩阵，$A$ 和 $B$ 分别是维度为 $\text{op}(A)$ $n \times k$ 和 $\text{op}(B)$ $n \times k$ 的矩阵。另外，对于矩阵 $A$ 和 $B$

$\text{op(}A\text{) and op(}B\text{)} = \left\{ \begin{matrix}
{A\text{ and }B} & {\text{if }\textsf{trans == CUBLAS\_OP\_N}} \\
{A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == CUBLAS\_OP\_C}} \\
\end{matrix} \right.$

当矩阵 B 以确保结果为厄米特的方式给出时，可以使用此例程。一个常见的例子是当矩阵 B 是矩阵 A 的缩放形式：这等价于 B 是矩阵 A 与对角矩阵的乘积。有关高效计算普通矩阵与对角矩阵的乘积，请参阅例程 cublas<t>dgmm()。

| 参数 | 内存 | 输入/输出 | 含义 |
| --- | --- | --- | --- |
| handle |  | input | cuBLAS 库上下文的句柄。 |
| uplo |  | input | 指示矩阵 C 的下三角或上三角部分是否存储，另一厄米特部分不被引用。 |
| trans |  | input | 运算 op(A)，为非转置或（共轭）转置。 |
| n |  | input | 矩阵 op(A)、op(B) 和 C 的行数。 |
| k |  | input | 矩阵 op(A) 和 op(B) 的列数。 |
| alpha | host 或 device | input | 用于乘法的 <type> 标量。 |
| A | device | input | <type> 数组，维度为 lda × k，当 transa == CUBLAS_OP_N 时 lda >= max(1, n)，否则为 lda × n 且 lda >= max(1, k)。 |
| lda |  | input | 用于存储矩阵 A 的二维数组的主维。 |
| B | device | input | <type> 数组，维度为 ldb × k，当 transb == CUBLAS_OP_N 时 ldb >= max(1, n)，否则为 ldb × n 且 ldb >= max(1, k)。 |
| ldb |  | input | 用于存储矩阵 B 的二维数组的主维。 |
| beta | host 或 device | input | 用于乘法的实数标量。如果 beta == 0