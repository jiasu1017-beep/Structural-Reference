# *CFLUX






### *CFLUX在热传递或质量扩散分析中指定集中通量。

此选项用于在完全耦合热应力分析中向模型的任何节点施加通量。在 Abaqus/Standard 中，它还用于热传递、耦合热-电、耦合热-电-结构和质量扩散分析。

**产品：**Abaqus/Standard  Abaqus/Explicit  Abaqus/CAE  

**类型：**历史数据 

**级别：**步骤

**Abaqus/CAE: **载荷模块

##### **参考：**

- ["热载荷，" Abaqus 分析用户指南第 34.4.4 节](../usb/usb-link.md#usb-prc-pthermal)

### **从输出数据库文件读取集中节点通量的必需参数：**

FILE

将此参数设置为要从中读取数据的输出数据库文件名。文件扩展名是可选的。

### **可选参数：**

AMPLITUDE

将此参数设置为在步骤中定义通量大小的幅值曲线名称（["幅值曲线，" Abaqus 分析用户指南第 34.1.2 节](../usb/usb-link.md#usb-prc-pamplitude)）。

如果在 Abaqus/Standard 分析中省略此参数，则根据 [*STEP](ch18abk36.md) 选项上 AMPLITUDE 参数分配的值，参考大小将在步骤开始时立即应用或在线性分布于整个步骤（请参阅 ["定义分析，" Abaqus 分析用户指南第 6.1.2 节](../usb/usb-link.md#usb-anl-aover)）。如果在 Abaqus/Explicit 分析中省略此参数，参考大小将在步骤开始时立即应用。

INC

仅当使用 FILE 参数时此参数才相关。

将此参数设置为将从中读取集中节点通量的先前分析所选步骤中的增量数。默认情况下，集中节点通量将从 STEP 参数指定的步骤的最后增量读取，如果省略 STEP 参数，则从最后步骤读取。

OP

设置 OP=MOD（默认）以保留现有的 [*CFLUX](ch03abk16.md)，此选项修改现有的通量或定义额外的通量。

设置 OP=NEW 以移除模型上所有现有的 [*CFLUX](ch03abk16.md)。

REGION TYPE

此参数仅适用于 Abaqus/Explicit 分析。

此参数仅适用于施加在自适应网格域边界上的集中通量。如果集中通量施加在自适应网格域内部的节点上，这些节点将始终跟随材料移动。

设置 REGION TYPE=LAGRANGIAN（默认）以将集中通量施加到跟随材料（非自适应）的节点。

设置 REGION TYPE=SLIDING 以将集中通量施加到可以在材料上滑动的节点。通常对节点施加网格约束以将其空间固定。

设置 REGION TYPE=EULERIAN 以将集中通量施加到可以独立于材料移动的节点。此选项仅用于材料可以流入或流出自适应网格域的边界区域。必须沿欧拉边界区域使用网格约束法向，以允许材料流过该区域。如果没有施加网格约束，欧拉边界区域的行为将与滑动边界区域相同。

STEP

仅当使用 FILE 参数时此参数才相关。

将此参数设置为将从中读取集中节点通量的先前分析的步骤号。默认情况下，集中节点通量将从先前分析的最后步骤读取。

### **用于定义集中通量的数据行：**

**第一行：**

根据需要重复此数据行，以在不同节点和自由度上定义集中通量。




