# 47.4.7 对X–Y数据对象使用乘法运算







### 47.4.7 对X–Y数据对象使用乘法运算

使用数学 ***** 符号对[之前保存的](pt05ch47s02hlb07.md) *X–Y* 数据对象（有序对的集合）进行运算，生成一个新的 *X–Y* 数据对象。***** 运算符接受两个交换参数，可以采用以下两种方式之一应用：

**将标量与X–Y数据对象相乘：**

此方法生成一个新的 *X–Y* 数据对象，其 *X* 坐标值与原始 *X–Y* 数据对象相同。Abaqus/CAE 将新的 *Y* 坐标计算为标量乘以每个原始 *Y* 坐标。例如，如果

![](../graphics/usi_eqn00745.gif)

那么

![](../graphics/usi_eqn00746.gif)

**将X–Y数据对象与另一个X–Y数据对象相乘：**

此方法生成一个新的 *X–Y* 数据对象，其 *X* 坐标为第一个数据对象的所有 *X* 坐标以及对齐两个对象所需的任何其他 *X* 坐标。Abaqus/CAE 通过[插值和外推](pt05ch47s03s02.md)计算额外的 *X–Y* 数据对。新的数据对象以其 *Y* 坐标作为第一个数据对象的 *Y* 坐标乘以第二个数据对象的 *Y* 坐标。例如，设

![](../graphics/usi_eqn00747.gif)

和

![](../graphics/usi_eqn00732.gif)

为了对齐，Abaqus/CAE 计算第一个和第二个数据对象的值：

![](../graphics/usi_eqn00748.gif)

和

![](../graphics/usi_eqn00734.gif)

然后

![](../graphics/usi_eqn00749.gif)

[图47-4](pt05ch47s03hlb05.md#usv-xyplot-multiply) 显示了上述示例的 *X–Y* 曲线图。

**图47-4** 说明数据对象乘法的 *X–Y* 曲线图。

![](../graphics/usv-xyplot-multiply.png)

**对X–Y数据对象使用乘法运算：**

1. 找到 **Operate on XY Data** 对话框。从主菜单栏，选择 ****Tools**![](../graphics/images/arrow.gif)**XY Data**![](../graphics/images/arrow.gif)**Create****. 在出现的对话框中点击 **Operate on XY data**；然后点击 **Continue**。**Operate on XY Data** 对话框出现。
2. 从 **XY Data** 选项中，点击要操作的 *X–Y* 数据对象的名称，然后点击 **Add to Expression**。您可以从本次会话中之前保存的所有 *X–Y* 数据对象中选择（在 **XY Data** 字段中按字母顺序列出）。*X–Y* 数据对象名称出现在表达式窗口中。
3. 从列出的 **Operators** 中，点击 *****。***** 符号出现在表达式窗口中数据对象名称之后。
4. 要指定第二个参数，请执行以下操作之一：- 使用鼠标和键盘在表达式窗口中输入标量作为 ***** 运算符的第二个参数，或 - 从 **XY Data** 选项中，点击表达式窗口中 ***** 运算符的数据对象参数的名称，然后点击 **Add to Expression**。
5. 要继续构建表达式，将光标定位在表达式窗口中，然后输入或选择要包含的函数、运算符和 *X–Y* 数据。
6. 要评估并显示表达式，点击 **Plot Expression**。
7. 要[保存](pt05ch47s02hlb07.md)您的新 *X–Y* 数据对象，点击 **Save As**，然后在出现的对话框中提供名称。保存数据对象使其可用于本次会话中的未来操作以及包含多个数据对象的 *X–Y* 曲线图。
8. 完成后，点击 **Cancel** 关闭对话框。

![](../graphics/images/black4rule.gif)有关相关信息，请点击以下任一项目：- ["理解X–Y数据插值和外推，" 第47.4.2节](pt05ch47s03s02.md)
- ["X–Y数据操作概述，" 第47.4.4节](pt05ch47s03hlb02.md)






