# 21.1 Field 对象





Field 对象是其他 Field 对象的抽象基类型。Field 对象没有显式构造函数。Field 对象的方法和成员对所有派生自 Field 的对象都是通用的。

**访问**

```
import fields
```

### 21.1.1 成员

Field 对象可以具有以下成员：

*name*

一个 String，指定存储库键。

*description*

一个 String，指定场的描述。默认值为空字符串。





