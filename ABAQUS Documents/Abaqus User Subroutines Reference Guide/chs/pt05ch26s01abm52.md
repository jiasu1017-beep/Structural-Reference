# 26.1.2 热膨胀

**产品：** Abaqus/Standard  Abaqus/Explicit  Abaqus/CFD  Abaqus/CAE

##### **参考资料**

- ["材料库：概述，" 第21.1.1节](pt05ch21s01abo18.md)
- ["UEXPAN，" Abaqus用户子程序参考指南第1.1.30节](../sub/sub-link.md#sub-rtn-uuexpan)
- [*EXPANSION](../key/key-link.md#usb-kws-mexpansion)
- ["定义其他机械模型，" Abaqus/CAE用户指南第12.9.4节](../usi/usi-link.md#usi-prp-mechanical-other)
- ["定义充液多孔材料，" Abaqus/CAE用户指南第12.12.3节](../usi/usi-link.md#usi-prp-other-porefluid)

### 概述

热膨胀效应：
- 可以通过指定热膨胀系数来定义，以便Abaqus可以计算热应变，以及在Abaqus/CFD中计算浮力；
- 可以是各向同性、正交各向异性或完全各向异性；
- 定义为从参考温度起的总膨胀；
- 可以指定为温度和/或场变量的函数；
- 可以在Abaqus/Standard中为固体连续体元素使用分布定义；以及
- 在Abaqus/Standard中可以直接在用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)中指定（如果热应变是场变量和状态变量的复杂函数）。

### 定义热膨胀系数

热膨胀是包含在材料定义中的材料属性（参见["材料数据定义，" 第21.1.2节](pt05ch21s01aus109.md)），但当它指的是其材料属性未作为材料定义一部分的垫圈的膨胀时除外。在这种情况下，膨胀必须与垫圈行为定义结合使用（参见["使用垫圈行为模型直接定义垫圈行为，" 第32.6.6节](pt06ch32s06alm51.md)）。

在Abaqus/Standard分析中，可以通过使用分布（["分布定义，" 第2.8.1节](pt01ch02s08aus26.md)）为均质固体连续体元素定义空间变化的热膨胀。分布必须包含热膨胀的默认值。如果使用分布，则不能定义热膨胀对温度和/或场变量的依赖性。

在Abaqus/CFD分析中，可以定义热膨胀系数，![](../graphics/usb_eqn00156.gif)，用于计算固体材料中的热应变，并且可以定义体积热膨胀系数，![](../graphics/usb_eqn00177.gif)，用于计算流体材料中的浮力。详见下面["热应变计算](pt05ch26s01abm52.md#usb-mat-cthermalstrains)”和["Abaqus/CFD中浮力计算](pt05ch26s01abm52.md#usb-mat-cthermalbuoyancy)”中对每个系数的详细说明。

| **输入文件用法：** | 使用以下选项为大多数材料定义热膨胀： |
| --- | --- |
|  | ``` [*MATERIAL](../key/key-link.md#usb-kws-mmaterial) [*EXPANSION](../key/key-link.md#usb-kws-mexpansion) ``` 使用以下选项为其本构响应直接定义为垫圈行为的垫圈定义热膨胀： ``` [*GASKET BEHAVIOR](../key/key-link.md#usb-kws-mgasketbehavior) [*EXPANSION](../key/key-link.md#usb-kws-mexpansion) ``` |

| **Abaqus/CAE用法：** | 使用以下选项与其他材料行为（包括垫圈行为）结合以包含热膨胀效应： |
| --- | --- |
|  | 属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀**** |

#### 热应变计算

Abaqus需要热膨胀系数，![](../graphics/usb_eqn00156.gif)，它定义了从参考温度，![](../graphics/usb_eqn04113.gif，起的总热膨胀，如图26.1.2-1所示（[图26.1.2-1](pt05ch26s01abm52.md#cthermalexpan-def)）。

**图26.1.2-1** 热膨胀系数的定义。

![](../graphics/cthermalexpan-def.png)

它们根据公式产生热应变

![](../graphics/usb_eqn04114.gif)

其中

![](../graphics/usb_eqn04115.gif)

是热膨胀系数；

![](../graphics/usb_eqn00123.gif)

是当前温度；

![](../graphics/usb_eqn04116.gif)

是初始温度；

![](../graphics/usb_eqn04117.gif)

是预定义场变量的当前值；

![](../graphics/usb_eqn04118.gif)

是场变量的初始值；以及

![](../graphics/usb_eqn00808.gif)

是热膨胀系数的参考温度。

上述方程中的第二项表示由初始温度，![](../graphics/usb_eqn04116.gif，与参考温度，![](../graphics/usb_eqn00808.gif，之间的差异引起的应变。当参考温度不等于初始温度时，需要此项来强制执行没有初始热应变的假设。

##### 定义参考温度

如果热膨胀系数，![](../graphics/usb_eqn00156.gif，不是温度或场变量的函数，则不需要参考温度，![](../graphics/usb_eqn00808.gif，的值。如果![](../graphics/usb_eqn00156.gif)是温度或场变量的函数，则可以定义![](../graphics/usb_eqn00808.gif。

| **输入文件用法：** | ``` [*EXPANSION](../key/key-link.md#usb-kws-mexpansion), ZERO=![](../graphics/usb_eqn00808.gif) ``` |
| --- | --- |

| **Abaqus/CAE用法：** | 属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀****：** 参考温度：** ![](../graphics/usb_eqn00808.gif) |
| --- | --- |

#### Abaqus/CFD中浮力的计算

在Abaqus/CFD流体中驱动自然对流的浮力使用Boussinesq近似计算

![](../graphics/usb_eqn04119.gif)

其中

![](../graphics/usb_eqn00347.gif)

是密度；

![](../graphics/usb_eqn04120.gif)

是初始密度；

![](../graphics/usb_eqn04121.gif)

是重力加速度；

![](../graphics/usb_eqn00123.gif)

是温度；

![](../graphics/usb_eqn00808.gif)

是参考温度；以及

![](../graphics/usb_eqn00177.gif)

是体积热膨胀系数。

体积热膨胀系数，![](../graphics/usb_eqn00177.gif，定义为

![](../graphics/usb_eqn04122.gif)

并且通过表达式与热膨胀系数，![](../graphics/usb_eqn00156.gif，相关

![](../graphics/usb_eqn04123.gif)

#### 将热膨胀系数从微分形式转换为总形式

总热膨胀系数通常在材料属性表中可用。但是，有时您会获得微分形式的热膨胀数据：

![](../graphics/usb_eqn04124.gif)

也就是说，提供了应变-温度曲线的切线（参见[图26.1.2-1](pt05ch26s01abm52.md#cthermalexpan-def)）。要转换为Abaqus所需的总热膨胀形式，必须从适当选择的参考温度，![](../graphics/usb_eqn00808.gif，积分这个关系：

![](../graphics/usb_eqn04125.gif)

例如，假设![](../graphics/usb_eqn04126.gif是一系列恒定值：![](../graphics/usb_eqn04127.gif在![](../graphics/usb_eqn00808.gif和![](../graphics/usb_eqn04128.gif之间；![](../graphics/usb_eqn04129.gif在![](../graphics/usb_eqn04128.gif和![](../graphics/usb_eqn04130.gif之间；![](../graphics/usb_eqn04131.gif在![](../graphics/usb_eqn04130.gif和![](../graphics/usb_eqn04132.gif之间；等等。那么，

![](../graphics/usb_eqn04133.gif)

Abaqus所需的总膨胀系数随后获得为

![](../graphics/usb_eqn04134.gif)

### 在用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)中定义热应变增量

在Abaqus/Standard用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)中，可以将热应变增量指定为温度和/或预定义场变量的函数。如果热应变增量取决于状态变量，则必须使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)。

| **输入文件用法：** | ``` [*EXPANSION](../key/key-link.md#usb-kws-mexpansion), USER ``` |
| --- | --- |

| **Abaqus/CAE用法：** | 属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀****：** 使用用户子程序UEXPAN** |
| --- | --- |

### 定义初始温度和场变量值

如果热膨胀系数，![](../graphics/usb_eqn00156.gif，是温度或场变量的函数，则初始温度和初始场变量值，![](../graphics/usb_eqn04116.gif和![](../graphics/usb_eqn04135.gif，如["Abaqus/Standard和Abaqus/Explicit中的初始条件，" 第34.2.1节](pt07ch34s02aus116.md中所述给出。

#### 单元移除和重新激活

如果在Abaqus/Standard中移除了一个单元随后又重新激活它（["单元和接触对的移除和重新激活，" 第11.2.1节](pt04ch11s02aus66.md)），则在热应变方程中的![](../graphics/usb_eqn04116.gif和![](../graphics/usb_eqn04118.gif表示重新激活时刻的温度和场变量值。

### 定义方向相关热膨胀

可以在Abaqus中定义各向同性或正交各向异性热膨胀。此外，可以在Abaqus/Standard中定义完全各向异性热膨胀。

正交各向异性和各向异性热膨胀只能与材料方向使用局部方向定义的材料一起使用（参见["方向，" 第2.2.5节](pt01ch02s02aus15.md)）。

Abaqus/Explicit中的正交各向异性热膨胀仅允许与各向异性弹性（包括正交各向异性弹性）和各向异性屈服一起使用（参见["各向异性屈服/蠕变，" 第23.2.6节](pt05ch23s02abm22.md)）。

在Abaqus/CFD中、绝热应力分析以及超弹性和超泡沫材料模型中，仅允许各向同性热膨胀。

#### 各向同性膨胀

如果直接定义热膨胀系数，则在每个温度下只需要一个![](../graphics/usb_eqn00156.gif值。如果使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)，则只需定义一个各向同性热应变增量（![](../graphics/usb_eqn04136.gif）。

| **输入文件用法：** | 使用以下选项直接定义热膨胀系数： |
| --- | --- |
|  | ``` [*EXPANSION](../key/key-link.md#usb-kws-mexpansion), TYPE=ISO ``` 使用以下选项使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)定义热膨胀： ``` [*EXPANSION](../key/key-link.md#usb-kws-mexpansion), TYPE=ISO, USER ``` |

| **Abaqus/CAE用法：** | 使用以下输入直接定义热膨胀系数： |
| --- | --- |
|  | 属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀****：** 类型：各向同性** 使用以下输入使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)定义热膨胀：属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀****：** 类型：各向同性**，**使用用户子程序UEXPAN** |

#### 正交各向异性膨胀

如果直接定义热膨胀系数，则应将主材料方向上的三个膨胀系数（![](../graphics/usb_eqn04137.gif、![](../graphics/usb_eqn04138.gif和![](../graphics/usb_eqn04139.gif）给为温度的函数。如果使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)，则必须定义主材料方向上热应变增量的三个分量（![](../graphics/usb_eqn04140.gif、![](../graphics/usb_eqn04141.gif和![](../graphics/usb_eqn04142.gif）。

| **输入文件用法：** | 使用以下选项直接定义热膨胀系数： |
| --- | --- |
|  | ``` [*EXPANSION](../key/key-link.md#usb-kws-mexpansion), TYPE=ORTHO ``` 使用以下选项使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)定义热膨胀： ``` [*EXPANSION](../key/key-link.md#usb-kws-mexpansion), TYPE=ORTHO, USER ``` |

| **Abaqus/CAE用法：** | 使用以下输入直接定义热膨胀系数： |
| --- | --- |
|  | 属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀****：** 类型：正交各向异性** 使用以下输入使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)定义热膨胀：属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀****：** 类型：正交各向异性**，**使用用户子程序UEXPAN** |

#### 各向异性膨胀

如果直接定义热膨胀系数，则必须将![](../graphics/usb_eqn00156.gif的所有六个分量（![](../graphics/usb_eqn04137.gif、![](../graphics/usb_eqn04138.gif、![](../graphics/usb_eqn04139.gif、![](../graphics/usb_eqn04143.gif、![](../graphics/usb_eqn04144.gif、![](../graphics/usb_eqn04145.gif）给为温度的函数。如果使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)，则必须定义热应变增量的所有六个分量（![](../graphics/usb_eqn04140.gif、![](../graphics/usb_eqn04141.gif、![](../graphics/usb_eqn04142.gif、![](../graphics/usb_eqn04146.gif、![](../graphics/usb_eqn04147.gif、![](../graphics/usb_eqn04148.gif）。

在Abaqus/Standard分析中，如果使用分布定义热膨胀，则分布中为每个元素给定的膨胀系数数量（由相关分布表确定，参见["分布定义，" 第2.8.1节](pt01ch02s08aus26.md)）必须与为膨胀行为指定的各向异性水平一致。例如，如果指定了正交各向异性行为，则必须为分布中的每个元素定义三个膨胀系数。

| **输入文件用法：** | 使用以下选项直接定义热膨胀系数： |
| --- | --- |
|  | ``` [*EXPANSION](../key/key-link.md#usb-kws-mexpansion), TYPE=ANISO ``` 使用以下选项使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)定义热膨胀： ``` [*EXPANSION](../key/key-link.md#usb-kws-mexpansion), TYPE=ANISO, USER ``` |

| **Abaqus/CAE用法：** | 使用以下输入直接定义热膨胀系数： |
| --- | --- |
|  | 属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀****：** 类型：各向异性** 使用以下输入使用用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)定义热膨胀：属性模块：材料编辑器：****机械**![](../graphics/images/arrow.gif)**膨胀****：** 类型：各向异性**，**使用用户子程序UEXPAN** |

### 热应力

当结构不能自由膨胀时，温度变化会引起应力。例如，考虑一个长度为L的两节点桁架，两端完全约束。横截面积；杨氏模量，*E*；和热膨胀系数，![](../graphics/usb_eqn00156.gif，都是常数。这个一维问题的应力可以根据胡克定律计算为![](../graphics/usb_eqn04149.gif，其中![](../graphics/usb_eqn04150.gif是总应变，![](../graphics/usb_eqn04151.gif是热应变，其中![](../graphics/usb_eqn04152.gif是温度变化。由于元素完全约束，![](../graphics/usb_eqn04153.gif。如果两个节点的温度相同，我们得到应力![](../graphics/usb_eqn04154.gif。

约束热膨胀会引起显著的应力。对于典型的结构金属，约150°C（300°F）的温度变化可能导致屈服。因此，对于涉及热加载的问题，定义边界条件时通常需要特别小心，以避免过度约束热膨胀。

#### 能量平衡考虑

Abaqus不在总能量平衡方程中考虑热膨胀效应，这可能导致模型总能量明显不平衡。例如，在上面两端约束的两节点桁架示例中，约束热膨胀引入的应变能将导致模型总能量的等效增加。

### 与其他材料属性或行为一起使用

在Abaqus中，热膨胀可以与任何其他（机械）材料（参见["组合材料行为，" 第21.1.3节](pt05ch21s01aus110.md)）结合使用。

#### 将热膨胀与其他材料模型一起使用

对于大多数材料，热膨胀由单个系数或一组正交各向异性或各向异性系数定义，或者在Abaqus/Standard中，通过在用户子程序[`UEXPAN`](../sub/sub-link.md#sub-xsl-uexpan)中定义增量热应变来定义。对于Abaqus/Standard中的多孔介质（如土壤或岩石），可以为固体颗粒和渗透流体定义热膨胀（当使用耦合孔隙流体扩散/应力程序时——参见["耦合孔隙流体扩散和应力分析，" 第6.8.1节](pt03ch06s08at26.md)）。在这种情况下，应重复热膨胀定义以定义不同的热膨胀效应。

#### 将热膨胀与垫圈行为一起使用

热膨胀可以与任何垫圈行为定义结合使用。热膨胀将影响垫圈在膜方向上的膨胀和/或垫圈厚度方向上的膨胀。

### 单元

热膨胀可用于Abaqus中的任何应力/位移或流体单元。
