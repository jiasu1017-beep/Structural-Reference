# 40.12 OdbDataInstance 对象

OdbDataInstance 对象实例数据。

**访问**

```
import visualization
session.odbData[*name*].instances[*i*]
```

### 40.12.1 成员

OdbDataInstance 对象具有以下成员：

*name*

 String，指定实例名称。此属性为只读。

