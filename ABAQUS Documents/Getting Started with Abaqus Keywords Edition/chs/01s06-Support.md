# 1.6 有限元方法快速回顾

本节回顾有限元方法的基础知识。任何有限元模拟的第一步都是使用一组**有限单元**对实际结构几何进行**离散化**。每个有限单元代表物理结构的一个离散部分。有限单元通过共享的**节点**连接。节点和有限单元的集合称为**网格**。网格中每单位长度、面积或体积的元素数量称为**网格密度**。在应力分析中，节点的位移是 Abaqus 计算的基本变量。一旦已知节点位移，就可以轻松确定每个有限单元中的应力和应变。

## 1.6.1 使用隐式方法获取节点位移

如图 1-3 所示，一端约束、另一端受载的简单桁架示例用于介绍本文档中使用的一些术语和约定。

**图 1-3** 桁架问题。

![../graphics/gss-truss.png](../graphics/gss-truss.png)

分析的目标是找到桁架自由端的位移、桁架中的应力以及桁架约束端的反作用力。

在这种情况下，图 1-3 中所示的杆将用两个桁架单元建模。在 Abaqus 中，桁架单元只能承受轴向载荷。离散化模型及其节点和单元标签如图 1-4 所示。

**图 1-4** 桁架问题的离散化模型。

![../graphics/gss-discretized-nls.png](../graphics/gss-discretized-nls.png)

模型中每个节点的自由体图如图 1-5 所示。通常，每个节点承受作用于模型的外部载荷 *P*，以及由连接到该节点的单元中的应力引起的内部载荷 *I*。对于模型处于静力平衡状态，作用于每个节点上的净力必须为零；即，每个节点处的内部和外部载荷必须相互平衡。对于节点 *a*，平衡方程可如下获得。

**图 1-5** 每个节点的自由体图。

![../graphics/gss-free-body-nls.png](../graphics/gss-free-body-nls.png)

假设杆的长度变化很小，则单元 1 的应变由下式给出

![](../graphics/gsk_eqn00001.gif)

其中 ![](../graphics/gsk_eqn00002.gif) 和 ![](../graphics/gsk_eqn00003.gif) 分别是节点 *a* 和 *b* 的位移，*L* 是单元的原始长度。

假设材料是弹性的，则杆中的应力由应变乘以杨氏模量 *E* 给出：

![](../graphics/gsk_eqn00004.gif)

作用于端节点的轴向力等于杆中的应力乘以其横截面积 *A*。因此，获得内 力、材料特性和位移之间的关系：

![](../graphics/gsk_eqn00005.gif)

因此，节点 *a* 的平衡可写为

![](../graphics/gsk_eqn00006.gif)

节点 *b* 的平衡必须考虑连接在该节点处的两个单元作用的内力。单元 1 的内力现在沿相反方向作用，因此变为负值。结果方程为

![](../graphics/gsk_eqn00007.gif)

对于节点 *c*，平衡方程为

![](../graphics/gsk_eqn00008.gif)

对于隐式方法，平衡方程需要同时求解以获得所有节点的位移。这一要求最好通过矩阵技术实现；因此，将内部力和外部力贡献写成矩阵形式。如果两个单元的属性和尺寸相同，平衡方程可简化为：

![](../graphics/gsk_eqn00009.gif)

通常，单元刚度，即 ![](../graphics/gsk_eqn00010.gif) 项，可能因单元而异；因此，将单元刚度写为模型中两个单元的 ![](../graphics/gsk_eqn00011.gif) 和 ![](../graphics/gsk_eqn00012.gif)。我们感兴趣的是获得外部施加的力 *P* 与内部产生的力 *I* 平衡的平衡方程的解。在讨论该方程与收敛性和非线性的关系时，我们将其写为

![](../graphics/gsk_eqn00013.gif)

对于完整的双单元、三节点结构，因此修改符号并重写平衡方程为

![](../graphics/gsk_eqn00014.gif)

在隐式方法中（如 Abaqus/Standard 中使用的方法），可以求解该方程组以获得三个未知变量的值：![](../graphics/gsk_eqn00003.gif)、![](../graphics/gsk_eqn00015.gif) 和 ![](../graphics/gsk_eqn00016.gif)（![](../graphics/gsk_eqn00002.gif) 在问题中指定为 0.0）。一旦已知位移，我们就可以回过头来用它们计算桁架单元中的应力。隐式有限元方法要求在每个求解增量结束时求解一组方程。

与隐式方法相反，显式方法（如 Abaqus/Explicit 中使用的方法）不需要求解联立方程组或计算全局刚度矩阵。相反，解决方案从上一个增量运动地推进到下一个。将有限元方法扩展到显式动力学将在下一节中介绍。

## 1.6.2 应力波传播图示

本节试图提供一些概念性理解，介绍使用显式动力学方法时力如何通过模型传播。在这个说明性示例中，我们考虑沿杆传播的应力波，该杆用三个单元建模，如图 1-6 所示。我们将研究在时间推进过程中杆的状态。

**图 1-6** 具有集中载荷的杆的初始配置，*F* 在自由端。

![../graphics/gsx-initrodconfig.png](../graphics/gsx-initrodconfig.png)

在第一个时间增量中，节点 1 由于作用于其上的集中力 *F* 而具有加速度 *a*。加速度使节点 1 具有速度 *v*，进而使单元 1 产生应变率 *ε̇*。单元 1 中应变的增量 *Δε* 通过对增量 1 时间内的应变率积分获得。总应变 *ε* 是初始应变 *ε₀* 和应变增量之和。在这种情况下，初始应变为零。一旦计算出单元应变，通过应用材料本构模型即可获得单元应力 *σ*。对于线弹性材料，应力简单地等于弹性模量乘以总应变。此过程如图 1-7 所示。节点 2 和 3 在第一个增量中不移动，因为没有力作用于它们。

**图 1-7** 在增量 1 结束时具有集中载荷 *F* 的杆的配置。

![../graphics/gsx-inc1-rod-config.png](../graphics/gsx-inc1-rod-config.png)

在第二个增量中，单元 1 中的应力向与单元 1 关联的节点施加内部单元力，如图 1-8 所示。然后使用这些单元应力计算节点 1 和 2 的动力平衡。

**图 1-8** 增量 2 开始时杆的配置。

![../graphics/gsx-inc2-rod-config.png](../graphics/gsx-inc2-rod-config.png)

该过程继续进行，因此在第三个增量开始时，单元 1 和 2 中都有应力，节点 1、2 和 3 上都有力，如图 1-9 所示。该过程继续进行，直到分析达到所需的总时间。

**图 1-9** 增量 3 开始时杆的配置。

![../graphics/gsx-inc3-rod-config.png](../graphics/gsx-inc3-rod-config.png)