# 57.1 Profile 对象







Profile 对象定义梁截面的几何属性。Profile 是一个抽象基类型。

**访问**

```
sectionApi.profiles()[*name*]
```

### 57.1.1 成员

Profile 对象具有以下成员：

**原型**

```
odb_String name() const;
```

*name*

一个 odb_String，指定 repository 键。

