# 10.2.3 基于表面的子模型

**产品：** Abaqus/Standard  

##### **参考文献**

- ["子模型：概述，" 第 10.2.1 节](pt04ch10s02aus60.md)
- [*SUBMODEL](../key/key-link.md#usb-kws-msubmodel)
- [*DSLOAD](../key/key-link.md#usb-kws-hdsload)
- [Abaqus/CAE 用户指南第 38 章，"子模型"](../usi/usi-link.md#usi-adv-submodeling)

### 概述

基于表面的子模型技术：
- 可能无法提供与基于节点的子模型相同的精度；
- 仅应在基于节点的技术无法提供足够结果时使用；
- 限于 Abaqus/Standard 中一般静态过程的基于应力的实体到实体子模型（请参阅["静态应力分析，" 第 6.2.2 节"](pt03ch06s02at01.md)）；
- 根据从全局模型插值的应力场将表面牵引力应用于子模型表面；以及
- 可与位移的基于节点的子模型结合使用（请参阅["基于节点的子模型，" 第 10.2.2 节"](pt04ch10s02aus61.md)）。

### 执行基于表面的子模型分析

您的子模型分析由从全局模型分析获得的结果部分或完全驱动。全局模型的结果被插值到子模型边界相关部分上的表面。因此，局部区域边界处的响应由全局模型的解定义。驱动表面和施加到局部区域的任何载荷决定了子模型中的解。

仅当基于节点的技术无法提供足够结果时，才应使用基于表面的子模型。有关两种子模型技术的比较及其应用建议，请参阅["子模型：概述，" 第 10.2.1 节"](pt04ch10s02aus60.md)。

### 保存全局模型的结果

必须将结果保存在为将驱动变量插值到子模型边界表面所需的所有单元上。仅输出数据库可用于此目的。

在全局模型的每个步骤中，其解将用于驱动子模型，将应力结果写入输出数据库（请参阅["输出到输出数据库，" 第 4.1.3 节"](pt02ch04s01aus40.md)）。

| **输入文件用法：** | 同时使用以下两个选项： |
| --- | --- |
|  | ``` [*OUTPUT](../key/key-link.md#usb-kws-houtput), FIELD [*ELEMENT OUTPUT](../key/key-link.md#usb-kws-helementoutput) ``` |

| **Abaqus/CAE 用法：** | 步骤模块：****输出**![](../graphics/images/arrow.gif)**场输出请求**![](../graphics/images/arrow.gif)**创建**** |
| --- | --- |

#### 从子模型分析引用全局模型结果

您必须定义全局解结果的来源并提供输出数据库文件的名称；文件扩展名是可选的。如果省略文件扩展名，并且输出数据库文件存在，则 Abaqus 将正确选择扩展名。

| **输入文件用法：** | **abaqus** **job**=*submodel_input_file* **globalmodel**=`*global_output_database*` |
| --- | --- |

| **Abaqus/CAE 用法：** | 任何模块：****模型**![](../graphics/images/arrow.gif)**编辑属性**![](../graphics/images/arrow.gif)***submodel*****: **子模型**：**从作业读取数据**：`*global_output_database*` |
| --- | --- |

### 在子模型中指定驱动表面

指定驱动的基于单元的表面不会激活驱动表面载荷：必须通过指定适当的子模型分布表面载荷来激活它们。

必须在任何步骤中由应力驱动的子模型的所有表面小平面必须指定为驱动表面，因为表面列表在其初始定义之后不能扩展（即使在重启时也不能扩展）。但是，给定表面处的变量不必在所有步骤中被驱动：在特定步骤中选择哪些表面被驱动是作为子模型分布表面载荷定义的一部分做出的，稍后在["在子模型中定义驱动表面牵引力"](pt04ch10s02aus62.md#asubmodelstress-defining)中讨论。

**图 10.2.3-1** 放大的子模型。

![](../graphics/asubmodel-mag-surface-nls.png)

| **输入文件用法：** | ``` [*SUBMODEL](../key/key-link.md#usb-kws-msubmodel) *基于单元的结构表面列表* ``` |
| --- | --- |
|  | [*SUBMODEL](../key/key-link.md#usb-kws-msubmodel) 选项必须包含在子模型分析的模型定义部分的输入文件中。允许使用多个 [*SUBMODEL](../key/key-link.md#usb-kws-msubmodel) 选项；但是，在这种情况下，您必须确保在一个选项的数据行上指定的驱动表面与所有其他选项的数据行上指定的其他表面是分开且不同的。 |

| **Abaqus/CAE 用法：** | 载荷模块：**创建载荷**：为**类别**选择**其他**，为**所选步骤的类型**选择**子模型**：**驱动区域**：选择区域 |
| --- | --- |

#### 定义几何容差

几何容差用于定义子模型中的驱动基于单元的表面节点可以偏离全局模型外表面多远（因为该表面在全局未变形有限元模型中被插值）。默认情况下，子模型中的表面节点必须位于由全局模型平均单元尺寸乘以 0.05 计算的距离内。您可以更改容差，这在子模型驱动表面在更大程度上位于全局模型外表面外部的情况下很有用。但是，大于此默认值的容差可能导致显著更长的计算时间和对于明显位于全局模型外表面的驱动表面区域更低的精度。

您可以将几何容差定义为全局模型中平均单元尺寸的分数或作为模型所选长度单位的绝对距离。如果两个容差都定义了，Abaqus 使用更严格的容差。

| **输入文件用法：** | 使用以下选项将几何容差定义为绝对距离： |
| --- | --- |
|  | ``` [*SUBMODEL](../key/key-link.md#usb-kws-msubmodel), TYPE=SURFACE, ABSOLUTE EXTERIOR TOLERANCE=*tolerance* ``` 使用以下选项将几何容差定义为全局模型中平均单元尺寸的分数： ``` [*SUBMODEL](../key/key-link.md#usb-kws-msubmodel), TYPE=SURFACE, EXTERIOR TOLERANCE=*tolerance* ``` |

| **Abaqus/CAE 用法：** | 载荷模块：**创建载荷**为**类别**选择**其他**，为**所选步骤的类型**选择**子模型**：选择区域：**外部容差**：**绝对：** 或 **相对：** *tolerance* |
| --- | --- |

##### 实体到实体子模型中的外部容差

实体到实体子模型分析的外部容差如图 10.2.3-2 中的阴影区域所示。如果驱动表面节点与全局模型自由表面之间的距离在指定容差范围内，则将全局模型的解变量外推到子模型。

**图 10.2.3-2** 基于表面的子模型中的外部容差。

![](../graphics/asubmodel-ext-tol-solid-solid-surface-nls.png)

### 在子模型中定义驱动表面牵引力

实际的驱动表面牵引力在任何步骤中定义为子模型分布表面载荷。导致这些牵引力的应力是从全局分析的输出数据库文件获得的"驱动变量"。

来自将驱动子模型边界表面的全局模型单元的所有应力分量必须已写入输出数据库。它们将用于在驱动表面（作为非均匀分布表面载荷）的积分点处创建牵引力、剪切应力和法向应力。所有适用的应力分量在每个时间增量处计算并应用到表面积分点。

| **输入文件用法：** | ``` [*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL ``` |

| **Abaqus/CAE 用法：** | 载荷模块：**创建载荷**：为**类别**选择**其他**，为**所选步骤的类型**选择**子模型** |
| --- | --- |

#### 指定全局分析中的步骤编号

您指定全局模型历史中用于当前子模型分析步骤中驱动变量的步骤。

| **输入文件用法：** | ``` [*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, STEP=*step* ``` |

| **Abaqus/CAE 用法：** | 载荷模块：**创建载荷**：为**类别**选择**其他**，为**所选步骤的类型**选择**子模型**：选择区域：**全局步骤编号：** *step* |
| --- | --- |

#### 修改驱动表面牵引力集

您可以从步骤到步骤修改子模型分布表面载荷定义以更改全局步骤引用，可以移除表面载荷定义，也可以稍后重新引入它们（请参阅["施加载荷：概述，" 第 34.4.1 节"](pt07ch34s04aus120.md)）。不能向为子模型定义的驱动表面总集中添加新表面；该驱动表面集是模型定义的固定部分。

| **输入文件用法：** | 使用以下选项之一： |
| --- | --- |
|  | ``` [*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, OP=MOD [*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, OP=NEW ``` |

### 获得足够解精度的指南

与基于节点的子模型不同，基于表面的子模型在许多情况下可能提供不正确或误导性的子模型结果。这种风险源于用于将应力从全局模型插值到子模型的方法：
- 全局模型材料点应力被平滑并与全局模型节点相关联。
- 然后，这些全局模型节点定位的应力被插值到子模型表面积分点并作为牵引力施加。

此过程通常是非保守的，导致子模型牵引力场在平衡意义上不等同于全局模型应力场。

#### 建模指南

您可以通过遵循以下指南来提高准确性并获得合理的子模型解：
- 设计您的模型，使子模型表面在应力梯度相对较低的区域与全局模型相交。
- 设计您的模型，使子模型表面在单元尺寸均匀的区域与全局模型相交。在数据（`.dat`）文件中会提供警告消息，用于在看到显著不均匀单元尺寸分布的情况。

#### 检查您的结果

要了解您的建模方法是否产生合理准确的解，建议遵循以下指南：
- 比较子模型驱动表面上的应力分布与全局模型中的应力分布。您可以使用 Abaqus/CAE 可视化模块中的切割平面和路径图等工具查看全局模型中的应力分布。全局模型应力分布与子模型驱动表面上应力分布的一致程度通常表示子模型解的精度水平。
- 当在子模型中使用惯性释放在子模型未移除所有刚体模式的情况下，将惯性释 lx 力与子模型中的主要力水平进行比较。如果惯性释放力相对于主要力水平较大，则子模型结果可能不准确。

### 特殊注意事项

有几个值得注意的特殊注意事项。

#### 刚体模式的处理

当您仅使用基于表面的子模型来驱动子模型响应时，您的位移解将不是唯一的；您通常会遇到刚体模式和伴随的数值问题。您可以通过以下方式解决这些刚体模式：
- 在子模型分析中提供足够的基于节点的子模型位移边界条件定义，
- 在子模型分析中提供足够的边界条件定义，或
- 在子模型分析中提供惯性释放载荷定义（请参阅["惯性释放，" 第 11.1.1 节"](pt04ch11s01at37.md)）。

您可以根据需要并适合您的模型组合这些定义，以解决所有刚体模式。

#### 有限旋转情况

全局模型应力结果以全局坐标系存储在输出数据库中。子模型牵引力由这些应力和子模型中当前构型表面法线计算。因此，当您的全局模型结果涉及显著有限旋转时，除非您提供足够的基于节点的子模型位移边界条件定义以将相似的刚体旋转传递给子模型，否则您的子模型结果通常将不准确；仅使用基于表面的子模型定义不足以提供这些刚体运动。当子模型未正确旋转时，您可能还会遇到子模型中的收敛困难。

#### 塑性行为

当使用基于表面的子模型来驱动具有非弹性材料定义的子模型区域时，您可能会遇到刚体模式和伴随的数值问题。例如，如果子模型材料定义包括塑性并且子模型载荷导致超出材料强化定义的剪切带形成，则数值问题将阻止收敛（即，如果子模型载荷超过极限载荷容量）。在这些情况下应使用基于节点的子模型。

### 过程

仅允许静态过程。一般（可能是非线性的）和线性扰动步骤都可以用于子模型（请参阅["一般和线性扰动过程，" 第 6.1.3 节"](pt03ch06s01aus44.md)，了解一般和线性扰动步骤的讨论）。

#### 使用线性扰动分析在特定时间点获取解

在 Abaqus/Standard 中，可以使用子模型分析中的静态线性扰动过程来研究对应于全局解中特定时间点的子模型线性化响应。您可以选择全局分析步骤中用作计算驱动变量值基础的增量。如果您没有在静态线性扰动步骤中选择增量，则使用全局分析中所选步骤的最后一个增量作为计算驱动变量值的基础。您不能在一般子模型步骤中选择增量。

| **输入文件用法：** | ``` [*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, STEP=*step*, INC=*increment* ``` |

| **Abaqus/CAE 用法：** | 在 Abaqus/CAE 中不支持选择特定全局模型增量。 |
| --- | --- |

#### 混合一般和线性扰动步骤

在全局和子模型分析中混合一般步骤和线性扰动步骤是可能的。Abaqus 允许在子模型期间将一般分析步骤视为线性扰动步骤，反之亦然。

##### 示例：使用一般和线性扰动步骤的子模型

有关使用一般和线性扰动步骤的子模型示例，请考虑以下情况。全局分析包括静态预载荷——作为一般非线性分析步骤——然后是预载结构的特征模态和频率提取，然后是 5 秒模态动态响应分析的步骤：

```
[*STEP](../key/key-link.md#usb-kws-hstep)
** Apply preload
[*STATIC](../key/key-link.md#usb-kws-hstatic)
 0.1, 1.0
…
** Write out stress results for elements needed to
** interpolate to the submodel's surfaces
[*ELEMENT OUTPUT](../key/key-link.md#usb-kws-helementoutput), ELSET=DETAIL
 S
[*END STEP](../key/key-link.md#usb-kws-hendstep)
[*STEP](../key/key-link.md#usb-kws-hstep)
** Calculate modes and frequencies
[*FREQUENCY](../key/key-link.md#usb-kws-hfrequency)
…
** The [*ELEMENT OUTPUT](../key/key-link.md#usb-kws-helementoutput) option is repeated because
** this is the first linear perturbation step
[*ELEMENT OUTPUT](../key/key-link.md#usb-kws-helementoutput), ELSET=DETAIL
 U
[*END STEP](../key/key-link.md#usb-kws-hendstep)
[*STEP](../key/key-link.md#usb-kws-hstep)
** Dynamic response of preloaded system
[*MODAL DYNAMIC](../key/key-link.md#usb-kws-hmodaldyn)
 0.01, 5.0
…
[*END STEP](../key/key-link.md#usb-kws-hendstep)
```

我们希望研究模型局部部分的局部（可能是非线性）响应，该部分非常小以至于我们不需要在局部建模动态效应，因此可以执行两个静态分析步骤：
```
** Define submodel surfaces (driven surfaces)
[*SUBMODEL](../key/key-link.md#usb-kws-msubmodel),TYPE=SURFACE
PERIM
[*STEP](../key/key-link.md#usb-kws-hstep)
** Preload
[*STATIC](../key/key-link.md#usb-kws-hstatic)
 0.1, 1.0
[*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, STEP=1
…
[*END STEP](../key/key-link.md#usb-kws-hendstep)
[*STEP](../key/key-link.md#usb-kws-hstep)
** Local static response to global dynamic step
[*STATIC](../key/key-link.md#usb-kws-hstatic)
 0.01, 5.0
[*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, STEP=3
…
[*END STEP](../key/key-link.md#usb-kws-hendstep)
```

子模型分析为两个步骤请求一般（可能是非线性的）分析是完全可以接受的，而在全局分析中动态步骤是线性扰动步骤（模态动态始终是线性扰动分析）。有您负责判断此子模型功能的使用是否合理。例如，假设全局分析继续进行第四个一般非线性静态响应步骤：
```
[*RESTART](../key/key-link.md#usb-kws-mrestart), READ, STEP=3
** Read state at end of initial preload
** (could equally well use [*RESTART](../key/key-link.md#usb-kws-mrestart), READ, STEP=1)
[*STEP](../key/key-link.md#usb-kws-hstep)
** Add more preload
[*STATIC](../key/key-link.md#usb-kws-hstatic)
 0.2, 1.0
…
[*END STEP](../key/key-link.md#usb-kws-hendstep)
```

第四个一般分析步骤从一般分析步骤 1 结束时的状态开始，因为频率提取和模态动态步骤都是线性扰动步骤。但是，如果我们以相同的方式重启子模型分析，则解可能与全局模型解不可比：
```
[*RESTART](../key/key-link.md#usb-kws-mrestart), READ, STEP=2
** Read state at end of step 2
[*STEP](../key/key-link.md#usb-kws-hstep)
** Add more preload
[*STATIC](../key/key-link.md#usb-kws-hstatic)
 0.2, 1.0
[*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, STEP=4
…
[*END STEP](../key/key-link.md#usb-kws-hendstep)
```

子模型中的第二个步骤是一般分析步骤，响应可能是非线性的，从而改变模型的状态。一个有效的替代方案是在第一个步骤之后立即将步骤 4 响应应用于子模型：
```
[*RESTART](../key/key-link.md#usb-kws-mrestart), READ, STEP=1
** Read state at end of preload step
[*STEP](../key/key-link.md#usb-kws-hstep)
** Add more preload
[*STATIC](../key/key-link.md#usb-kws-hstatic)
 0.2, 1.0
[*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, STEP=4
…
[*END STEP](../key/key-link.md#usb-kws-hendstep)
```

### 载荷

在全局分析子模型区域中施加的任何载荷必须以常规方式在子模型分析中施加。您有责任正确地将此类载荷应用于子模型，以使它们与全局模型的载荷相对应。请参阅["施加载荷：概述，" 第 34.4.1 节"](pt07ch34s04aus120.md)，了解 Abaqus 中可用载荷的概述。

### 输出

在特定过程中通常可用的任何输出在子模型分析期间也可用（请参阅["Abaqus/Standard 输出变量标识符，" 第 4.2.1 节"](pt02ch04s02abv01.md) 和["Abaqus/Explicit 输出变量标识符，" 第 4.2.2 节"](pt02ch04s02xbv01.md)）。

如上所述，必须在全局分析中使用单元应力输出请求到输出数据库文件，以保存子模型边界处驱动变量的值。

### 输入文件模板

#### 全局分析：

```
[*HEADING](../key/key-link.md#usb-kws-mheading)
…
[*STEP](../key/key-link.md#usb-kws-hstep)
Step 1
[*STATIC](../key/key-link.md#usb-kws-hstatic) (*or* [*STATIC](../key/key-link.md#usb-kws-hstatic), *etc.*)
*Data line to define step time and control incrementation*
…
[*ELEMENT OUTPUT](../key/key-link.md#usb-kws-helementoutput)
S
[*OUTPUT](../key/key-link.md#usb-kws-houtput), FIELD
[*ELEMENT OUTPUT](../key/key-link.md#usb-kws-helementoutput)
S
[*END STEP](../key/key-link.md#usb-kws-hendstep)
```

#### 子模型分析：

```
[*HEADING](../key/key-link.md#usb-kws-mheading)
…
[*SUBMODEL](../key/key-link.md#usb-kws-msubmodel),TYPE=SURFACE, EXTERIOR TOLERANCE=*tolerance*
*List of all surfaces to be driven*
**
[*STEP](../key/key-link.md#usb-kws-hstep)
[*STATIC](../key/key-link.md#usb-kws-hstatic) (*or any other allowable procedure*)
*Data line to define step time and control incrementation.*
…[*DSLOAD](../key/key-link.md#usb-kws-hdsload), SUBMODEL, STEP=1
*Data lines listing surfaces to be driven in this step*
…
[*END STEP](../key/key-link.md#usb-kws-hendstep)
```
