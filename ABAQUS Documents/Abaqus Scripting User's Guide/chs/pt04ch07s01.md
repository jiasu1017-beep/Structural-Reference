# 7.1 模式概述

有两种类型的模式：

**表单模式**

表单模式提供独立对话框的界面。

**过程模式**

过程模式提供一个界面，使用提示区域引导用户完成一系列步骤，这些步骤从对话框或视口中的选择收集输入。

如果模式需要在当前视口中执行绘图或高亮显示，该模式必须是过程模式。因为Abaqus/CAE会高亮显示用户选择的对象，所以任何需要用户在视口中进行选择的模式也必须是过程模式。过程模式确保一次只有一个过程控制当前视口中的场景。如果两个不同的过程出于不同目的高亮显示模型的不同部分，结果显示将令人困惑并产生歧义。
