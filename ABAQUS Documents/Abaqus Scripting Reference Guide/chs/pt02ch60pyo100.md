# 60.101 UniaxialTestData 对象

UniaxialTestData 对象提供单轴测试数据（压缩和/或拉伸）。

**访问**

```
materialApi.materials()[*name*].hyperelastic().uniaxialTestData()
materialApi.materials()[*name*].hyperfoam().uniaxialTestData()
materialApi.materials()[*name*].lowDensityFoam()\
.uniaxialCompressionTestData()
materialApi.materials()[*name*].lowDensityFoam().uniaxialTensionTestData()
materialApi.materials()[*name*].mullinsEffect().uniaxialTests(*i*)
```

### 60.101.1 UniaxialTestData(...)

此方法创建一个 UniaxialTestData 对象。

**路径**

```
materialApi.materials()[*name*].hyperelastic().UniaxialTestData
materialApi.materials()[*name*].hyperfoam().UniaxialTestData
materialApi.materials()[*name*].lowDensityFoam().UniaxialTestData
materialApi.materials()[*name*].mullinsEffect().UniaxialTestData
```

**原型**

```
odb_UniaxialTestData&
UniaxialTestData(const odb_SequenceSequenceDouble& table,
                 odb_Union smoothing,
                 bool lateralNominalStrain,
                 bool temperatureDependency,
                 int dependencies);
```

**必需参数**

*table*

一个 odb_SequenceSequenceDouble，指定如下所述的项目。

**可选参数**

*smoothing*

字符串"NONE"或一个 Int，指定平滑值。如果 *smoothing*="NONE"，则不采用平滑。默认值为"NONE"。

*lateralNominalStrain*

一个布尔值，指定是否包含横向名义应变。默认值为 false。

*temperatureDependency*

一个布尔值，指定数据是否依赖温度。默认值为 false。

*dependencies*

一个整数，指定场变量依赖数量。默认值为 0。

**表数据**

对于超弹性材料模型，表数据指定以下内容：
- 名义应力，![](../graphics/ker_eqn00377.gif]。
- 名义应变，![](../graphics/ker_eqn00378.gif]。

对于超泡沫材料模型，表数据指定以下内容：
- 名义应力，![](../graphics/ker_eqn00334.gif]。
- 名义应变，![](../graphics/ker_eqn00378.gif]。
- 名义横向应变，![](../graphics/ker_eqn00379.gif]。默认值为 0。

对于低密度泡沫材料模型，表数据指定以下内容：
- 名义应力，![](../graphics/ker_eqn00377.gif]。
- 名义应变，![](../graphics/ker_eqn00378.gif]。
- 名义应变率，![](../graphics/ker_eqn00380.gif]。

**返回值**

一个 UniaxialTestData 对象。

**异常**

无。

### 60.101.2 成员

UniaxialTestData 对象的成员与 [UniaxialTestData](pt02ch60pyo101.md#ker-uniaxialtestdata-uniaxialtestdata-cpp) 方法的参数具有相同的名称和描述。

### 60.101.3 对应的分析关键字

| [*UNIAXIAL TEST DATA](../key/key-link.md#usb-kws-munitestdata) |
| --- |
