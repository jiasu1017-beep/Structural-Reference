# 25.16 ContactControl 对象

ContactControl 对象是其他 ContactControl 对象的抽象基类型。ContactControl 对象没有显式构造函数、成员或方法。

**访问**

```
import interaction
mdb.models[*name*].contactControls[*name*]
```

### 25.16.1 成员

ContactControl 对象没有成员。
