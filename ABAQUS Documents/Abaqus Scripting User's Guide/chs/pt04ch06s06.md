# 6.6 AFXTargets

Targets类似于关键字，因为它们自动保持与GUI的数据同步；但是，targets不参与命令处理。Targets通常用于监视GUI中与命令没有直接关系的某些窗口部件的值；例如，从"创建载荷"对话框中选择要创建的载荷类型。支持以下类型的targets：

- `AFXIntTarget(initialValue)`
- `AFXFloatTarget(initialValue)`
- `AFXStringTarget(initialValue)`
