# 19.6.2 重启分析所需的文件





要重启分析，必须在启动 Abaqus/CAE 会话的目录中提供先前分析创建的各种文件。

**Abaqus/Standard**

- 输出数据库（`.odb`）
- 重启文件（`.res`）
- 模型文件（`.mdl`）
- 零件文件（`.prt`）
- 状态文件（`.stt`）

**Abaqus/Explicit**

- 输出数据库（`.odb`）
- 重启文件（`.res`）
- 模型文件（`.mdl`）
- 包文件（`.pac`）
- 零件文件（`.prt`）
- 状态文件（`.abq` 和 `.stt`）
-