# 25.23 ContactStabilization 对象

ContactStabilization 对象是其他 ContactStabilization 对象的抽象基类型。ContactStabilization 对象没有显式构造函数、成员或方法。

**访问**

```
import interaction
mdb.models[*name*].contactStabilizations[*name*]
```

### 25.23.1 成员

ContactStabilization 对象没有成员。





