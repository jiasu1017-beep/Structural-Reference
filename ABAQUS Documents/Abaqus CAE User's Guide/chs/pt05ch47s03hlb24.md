# 47.4.25 将X–Y数据对象提升到幂







### 47.4.25 将X–Y数据对象提升到幂

使用 **power** 函数对[之前保存的](pt05ch47s02hlb07.md) *X–Y* 数据对象（有序对的集合）进行运算，生成一个新的 *X–Y* 数据对象。**power** 函数需要两个参数，其中至少有一个必须是 *X–Y* 数据对象。此函数可以采用三种方式应用：

**将标量提升到X–Y数据对象的幂：** **power**(scalar, data object name)。**

此方法生成一个新的 *X–Y* 数据对象，其 *X* 坐标值与原始 *X–Y* 数据对象相同。Abaqus/CAE 将新的 *Y* 坐标计算为标量提升到每个原始 *Y* 坐标的幂。例如，如果

![](../graphics/usi_eqn00761.gif)

那么

![](../graphics/usi_eqn00762.gif)

**将X–Y数据对象提升到标量的幂：** **power**(data object name, scalar)。**

此方法生成一个新的 *X–Y* 数据对象，其 *X* 坐标值与原始 *X–Y* 数据对象相同。Abaqus/CAE 将新的 *Y* 坐标计算为每个原始 *Y* 坐标提升到标量的幂。例如，如果

![](../graphics/usi_eqn00763.gif)

那么

![](../graphics/usi_eqn00764.gif)

**将X–Y数据对象提升到另一个X–Y数据对象的幂：** **power**(data object name, data object name)。**

此方法生成一个新的 *X–Y* 数据对象，其 *X* 坐标为第一个数据对象的所有 *X* 坐标以及对齐两个对象所需的任何其他 *X* 坐标。Abaqus/CAE 通过[插值和外推](pt05ch47s03s02.md)计算额外的 *X–Y* 数据对。新的数据对象以其 *Y* 坐标作为第一个数据对象的 *Y* 坐标提升到第二个数据对象的 *Y* 坐标。例如，设

![](../graphics/usi_eqn00765.gif)

和

![](../graphics/usi_eqn00766.gif)

为了对齐，Abaqus/CAE 计算第一个和第二个数据对象的值：

![](../graphics/usi_eqn00767.gif)

和

![](../graphics/usi_eqn00768.gif)

然后

![](../graphics/usi_eqn00769.gif)

[图47-22](pt05ch47s03hlb23.md#usv-xyplot-power) 显示了上述示例的曲线图。

**图47-22** 一个数据对象提升到另一个数据对象的 *X–Y* 曲线图。

![](../graphics/usv-xyplot-power.png)

**应用 **power** 函数：**

1. 找到 **Operate on XY Data** 对话框。从主菜单栏，选择 ****Tools**![](../graphics/images/arrow.gif)**XY Data**![](../graphics/images/arrow.gif)**Create****. 在出现的对话框中点击 **Operate on XY data**；然后点击 **Continue**。**Operate on XY Data** 对话框出现。
2. 从列出的 **Operators** 中，点击 **power(A,A)**。**power** 函数出现在表达式窗口中。
3. 从 **XY Data** 选项中，点击要操作的 *X–Y* 数据对象的名称，然后点击 **Add to Expression**。您可以从本次会话中之前保存的所有 *X–Y* 数据对象中选择（在 **XY Data** 字段中按字母顺序列出）。*X–Y* 数据对象名称出现在表达式窗口中 **power** 函数的括号内。
4. 要指定下一个参数，请执行以下操作之一：- 使用鼠标和键盘在表达式窗口中输入标量作为 **power** 函数的第一或第二个参数（取决于您要执行的运算），并用逗号分隔两个参数，或 - 从 **XY Data** 选项中，点击表达式窗口中 **power** 函数的数据对象参数的名称，然后点击 **Add to Expression**。
5. 要继续构建表达式，将光标定位在表达式窗口中，然后输入或选择要包含的函数、运算符和 *X–Y* 数据。
6. 要评估并显示表达式，点击 **Plot Expression**。
7. 要[保存](pt05ch47s02hlb07.md)您的新 *X–Y* 数据对象，点击 **Save As**，然后在出现的对话框中提供名称。保存数据对象使其可用于本次会话中的未来操作以及包含多个数据对象的 *X–Y* 曲线图。
8. 完成后，点击 **Cancel** 关闭对话框。

![](../graphics/images/black4rule.gif)有关相关信息，请点击以下任一项目：- ["计算X–Y数据对象的指数，" 第47.4.23节](pt05ch47s03hlb21.md)
- ["理解X–Y数据插值和外推，" 第47.4.2节](pt05ch47s03s02.md)
- ["X–Y数据操作概述，" 第47.4.4节](pt05ch47s03hlb02.md)






