# 47.4.26 对X–Y数据对象应用Butterworth滤波







### 47.4.26 对X–Y数据对象应用Butterworth滤波

使用 **butterworthFilter** 函数对[之前保存的](pt05ch47s02hlb07.md) *X–Y* 数据对象（有序对的集合）应用Butterworth滤波运算，生成一个新的 *X–Y* 数据对象。例如，此滤波运算可用于去除高频噪声。

Butterworth滤波器的传递函数显示在Abaqus Analysis User's Guide的["Output to the output database"中的"Filtering output and operating on output in Abaqus/Explicit"第4.1.3节](../usb/usb-link.md#usb-out-odboutput-filter)中。

[图47-23](pt05ch47s03hlb24.md#usv-xyplot-bfilter) 说明了可以使用 **butterworthFilter** 运算生成的 *X–Y* 曲线图类型。

**图47-23** 使用 **butterworthFilter** 运算生成的 *X–Y* 曲线图。

![](../graphics/usv-xyplot-bfilter.png)

**butterworthFilter** 函数需要两个参数：*X–Y* 数据对象的名称（*name*）和截止频率（*cutoffFrequency*），即滤波器衰减至少一半输入信号的频率。可选参数的说明如下：
- 要使用的滤波器阶数（*filterOrder*）。此参数必须是正偶数值；默认值为2。
- 指定在数据信号起点处计算的投影和预充电方法的符号常数（*startCondition*）。此参数的有效值为 ZERO（应用零常投影和预充电）；CONSTANT（应用等于 *X–Y* 数据对象中第一个数据点的常投影和预充电）；MIRROR（应用相当于围绕第一个数据点处的垂直线反射 *X–Y* 数据对象的投影和预充电）；REVERSE_MIRROR（应用相当于围绕第一个数据点处的垂直线和水平线反射 *X–Y* 数据对象的投影和预充电）；以及 TANGENTIAL（应用与前两个数据点相切的线性投影和预充电）。默认值为 CONSTANT。
- 指定在数据信号终点处计算的投影和预充电方法的符号常数（*endCondition*）。此参数的有效值为 ZERO（应用零常投影和预充电）；CONSTANT（应用等于 *X–Y* 数据对象中最后一个数据点的常投影和预充电）；MIRROR（应用相当于围绕最后一个数据点处的垂直线反射 *X–Y* 数据对象的投影和预充电）；REVERSE_MIRROR（应用相当于围绕最后一个数据点处的垂直线和水平线反射 *X–Y* 数据对象的投影和预充电）；以及 TANGENTIAL（应用与最后两个数据点相切的线性投影和预充电）。默认值为 CONSTANT。
- 指定插值方案的符号常数（*interpolation*）。此参数的有效值为 QUADRATIC（指定拉格朗日二阶插值方案）；CUBIC_SPLINE（指定三次样条插值方案）；和 LINEAR（指定线性插值方案）。默认值为 QUADRATIC。
- 到达第一个数据点之前的原始数据曲线的斜率（*startslope*）。此参数的默认值为0.0（水平斜率），仅在 interpolation=CUBIC_SPLINE 时使用。
- 越过最后一个数据点之后的原始数据曲线的斜率（*endslope*）。此参数的默认值为0.0（水平斜率），仅在 interpolation=CUBIC_SPLINE 时使用。
- 指定是否对滤波后数据执行反向传递（*backwardPass*）的布尔值。此参数的默认值为 True。当此参数设置为 False 时，*endCondition* 参数将被忽略。

您的 *X–Y* 数据对象必须具有恒定的时间步长才能被滤波。如果时间步长不是常数，Abaqus/CAE 通过[插值](pt05ch47s03s02.md)以恒定间隔计算额外点。Butterworth滤波的恒定时间步长由要滤波的 *X–Y* 数据对象中的最小时间步长定义。

**对X–Y数据对象应用Butterworth滤波：**

1. 找到 **Operate on XY Data** 对话框。从主菜单栏，选择 ****Tools**![](../graphics/images/arrow.gif)**XY Data**![](../graphics/images/arrow.gif)**Create****. 在出现的对话框中点击 **Operate on XY data**；然后点击 **Continue**。**Operate on XY Data** 对话框出现。
2. 从列出的 **Operators** 中，点击 **butterworthFilter(X,F)**。**butterworthFilter** 函数出现在表达式窗口中。
3. 从 **XY Data** 选项中，点击要操作的 *X–Y* 数据对象的名称，然后点击 **Add to Expression**。您可以从本次会话中之前保存的所有 *X–Y* 数据对象中选择（在 **XY Data** 字段中按字母顺序列出）。*X–Y* 数据对象名称出现在表达式窗口中 **butterworthFilter** 函数的括号内。
4. 将光标定位在表达式窗口中第二个逗号之前，然后输入截止频率的值。
5. 要继续构建表达式，将光标定位在表达式窗口中，然后输入或选择要包含的函数、运算符和 *X–Y* 数据。
6. 要评估并显示表达式，点击 **Plot Expression**。
7. 要[保存](pt05ch47s02hlb07.md)您的新 *X–Y* 数据对象，点击 **Save As**，然后在出现的对话框中提供名称。保存数据对象使其可用于本次会话中的未来操作以及包含多个数据对象的 *X–Y* 曲线图。
8. 完成后，点击 **Cancel** 关闭对话框。

![](../graphics/images/black4rule.gif)有关相关信息，请点击以下任一项目：- ["理解X–Y数据插值和外推，" 第47.4.2节](pt05ch47s03s02.md)
- ["X–Y数据操作概述，" 第47.4.4节](pt05ch47s03hlb02.md)
- ["对X–Y数据对象应用正弦-Butterworth滤波，" 第47.4.30节](pt05ch47s03hlb28.md)
- ["平滑X–Y数据对象，" 第47.4.31节](pt05ch47s03hlb29.md)
- ["Abaqus Analysis User's Guide"第4.1.3节"Output to the output database"中的"Filtering output and operating on output in Abaqus/Explicit"](../usb/usb-link.md#usb-out-odboutput-filter)






